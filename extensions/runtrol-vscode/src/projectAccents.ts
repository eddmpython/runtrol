import accents from "./projectAccents.json";

/// The same data is consumed without a compiler by the build-time terminal glyph projection.
export const PROJECT_ACCENTS: readonly string[] = Object.freeze(accents);
