{
  "$defs": {
    "AcquireControlParams": {
      "additionalProperties": false,
      "description": "Acquire control only if the caller still sees this exact live state.",
      "properties": {
        "expectedLifecycle": {
          "$ref": "#/$defs/LifecycleState",
          "description": "Lifecycle state visible when the user chose the action."
        },
        "expectedSessionGeneration": {
          "description": "Lifecycle generation visible when the user chose the action.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "expectedLifecycle",
        "expectedSessionGeneration"
      ],
      "type": "object"
    },
    "AdoptNativeSessionParams": {
      "additionalProperties": false,
      "description": "Adopt one exact native catalogue observation into Runtime supervision.",
      "properties": {
        "access": {
          "$ref": "#/$defs/SessionWorkspaceAccess",
          "description": "Writer collision posture for the working tree."
        },
        "adoptionToken": {
          "description": "Short-lived Runtime proof issued with the authorized native catalogue entry.",
          "type": "string"
        },
        "nativeSessionId": {
          "description": "Provider-owned opaque session identity returned unchanged.",
          "type": "string"
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned with the native catalogue entry."
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "workspace": {
          "description": "Exact canonical workspace returned with the native catalogue entry.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "providerId",
        "nativeSessionId",
        "workspace",
        "access",
        "adoptionToken"
      ],
      "type": "object"
    },
    "AppScope": {
      "description": "Public integration authority, separate from remote device scopes.",
      "oneOf": [
        {
          "const": "provider.read",
          "description": "Read provider descriptors and structural capabilities.",
          "type": "string"
        },
        {
          "const": "model.read",
          "description": "Read official model catalogues.",
          "type": "string"
        },
        {
          "const": "session.list",
          "description": "List managed session metadata under approved roots.",
          "type": "string"
        },
        {
          "const": "session.native.discover",
          "description": "Read official native-session catalogues.",
          "type": "string"
        },
        {
          "const": "session.output.read",
          "description": "Read bounded live normalized events.",
          "type": "string"
        },
        {
          "const": "session.start",
          "description": "Start sessions under approved roots.",
          "type": "string"
        },
        {
          "const": "session.resume",
          "description": "Resume or adopt sessions under approved roots.",
          "type": "string"
        },
        {
          "const": "session.input.write",
          "description": "Acquire control and submit caller-owned input.",
          "type": "string"
        },
        {
          "const": "session.stop",
          "description": "Interrupt or cool one controlled session.",
          "type": "string"
        },
        {
          "const": "approval.respond.low",
          "description": "Answer exact low-risk structured approvals.",
          "type": "string"
        },
        {
          "const": "approval.respond.high",
          "description": "Answer supported high-risk approvals under additional local policy.",
          "type": "string"
        },
        {
          "const": "session.delete",
          "description": "Forget a Runtime pointer without deleting provider state.",
          "type": "string"
        }
      ]
    },
    "ArchiveNativeSessionParams": {
      "additionalProperties": false,
      "description": "Archive one provider-native conversation through the provider's own surface.\n\nRuntime relays the opaque identity and stores nothing. A conversation Runtime currently supervises\nmust be forgotten before the provider-owned conversation can be archived.",
      "properties": {
        "nativeSessionId": {
          "description": "Provider-owned opaque session identity returned unchanged.",
          "type": "string"
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned with the native catalogue entry."
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "workspace": {
          "description": "Exact canonical workspace returned with the native catalogue entry.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "providerId",
        "nativeSessionId",
        "workspace"
      ],
      "type": "object"
    },
    "CapabilityFreshness": {
      "description": "Freshness of a capability map relative to the installed binary identity.",
      "oneOf": [
        {
          "const": "current",
          "description": "The binary identity and driver observations were revalidated for this request.",
          "type": "string"
        },
        {
          "const": "stale",
          "description": "A safe prior result is returned while a refresh is unavailable.",
          "type": "string"
        }
      ]
    },
    "CatalogueCoverage": {
      "description": "Honest native catalogue coverage for the current provider and root context.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "The official provider surface claims complete pagination for the current context.",
          "properties": {
            "kind": {
              "const": "complete",
              "type": "string"
            },
            "source": {
              "$ref": "#/$defs/CatalogueSource",
              "description": "Provenance of the catalogue."
            }
          },
          "required": [
            "kind",
            "source"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The official surface or required path filtering has a named structural limitation.",
          "properties": {
            "kind": {
              "const": "partial",
              "type": "string"
            },
            "source": {
              "$ref": "#/$defs/CatalogueSource",
              "description": "Provenance of the catalogue."
            },
            "why": {
              "description": "Safe stable explanation of the limitation.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "source",
            "why"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "No registered official enumerable provider surface exists.",
          "properties": {
            "kind": {
              "const": "unsupported",
              "type": "string"
            },
            "why": {
              "description": "Safe stable explanation of the absent capability.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "why"
          ],
          "type": "object"
        }
      ]
    },
    "CatalogueSource": {
      "description": "The official provider surface used for discovery.",
      "oneOf": [
        {
          "const": "officialProtocol",
          "description": "A provider-owned structured protocol method.",
          "type": "string"
        },
        {
          "const": "officialCli",
          "description": "A provider-owned structured CLI command.",
          "type": "string"
        },
        {
          "const": "providerStore",
          "description": "The provider's own store, read for the names of what it holds (identity, folder, the provider's own\ntitle and time), used when the provider publishes no listing surface. Never a conversation's content.",
          "type": "string"
        }
      ]
    },
    "ClientCapabilities": {
      "description": "Client features understood by the initial read-only revision.",
      "properties": {
        "opaqueEventExtensions": {
          "default": false,
          "description": "Whether the client preserves bounded unknown optional event extensions.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ClientInfo": {
      "description": "Safe client presentation metadata.",
      "properties": {
        "name": {
          "description": "Product display name.",
          "type": "string"
        },
        "version": {
          "description": "Product version text.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "ControlLease": {
      "additionalProperties": false,
      "description": "One opaque renewable control authority for one live session incarnation.",
      "properties": {
        "expiresAtMs": {
          "description": "Wall-clock expiry in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseGeneration": {
          "description": "Monotonic lease generation required on renewal and mutations.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque unguessable lease identity.",
          "type": "string"
        },
        "sessionGeneration": {
          "description": "Session lifecycle generation observed when the lease was acquired.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact controlled session."
        }
      },
      "required": [
        "leaseId",
        "sessionId",
        "sessionGeneration",
        "leaseGeneration",
        "expiresAtMs"
      ],
      "type": "object"
    },
    "ControlLeaseParams": {
      "additionalProperties": false,
      "description": "Renew or release one exact lease generation.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration"
      ],
      "type": "object"
    },
    "CoolSessionParams": {
      "additionalProperties": false,
      "description": "Cool one exact idle session while retaining its provider-native pointer.",
      "properties": {
        "expectedSessionGeneration": {
          "description": "Lifecycle generation visible when the user chose to cool the session.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "expectedSessionGeneration",
        "leaseId",
        "leaseGeneration"
      ],
      "type": "object"
    },
    "DeleteNativeSessionParams": {
      "additionalProperties": false,
      "description": "Delete one provider-native conversation through the provider's own surface.\n\nThe provider does the deleting; Runtime relays the request and stores nothing. Refused when the provider\npublishes no surface for it (`capabilityUnavailable`), and answered with the provider's own refusal\notherwise. A conversation Runtime currently supervises is not deleted under it: forget the supervised\npointer first.",
      "properties": {
        "nativeSessionId": {
          "description": "Provider-owned opaque session identity returned unchanged.",
          "type": "string"
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned with the native catalogue entry."
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "workspace": {
          "description": "Exact canonical workspace returned with the native catalogue entry.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "providerId",
        "nativeSessionId",
        "workspace"
      ],
      "type": "object"
    },
    "EnrollmentDecision": {
      "description": "Current enrollment decision.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Local approval has not happened yet.",
          "properties": {
            "state": {
              "const": "pending",
              "type": "string"
            }
          },
          "required": [
            "state"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Exact narrowed grant was approved.",
          "properties": {
            "grant": {
              "$ref": "#/$defs/IntegrationGrant",
              "description": "Grant ready for authenticated reconnect."
            },
            "state": {
              "const": "approved",
              "type": "string"
            }
          },
          "required": [
            "state",
            "grant"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The local operator denied the request.",
          "properties": {
            "state": {
              "const": "denied",
              "type": "string"
            }
          },
          "required": [
            "state"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The bounded decision window elapsed.",
          "properties": {
            "state": {
              "const": "expired",
              "type": "string"
            }
          },
          "required": [
            "state"
          ],
          "type": "object"
        }
      ]
    },
    "EnrollmentManifest": {
      "additionalProperties": false,
      "description": "Closed self-description and exact authority requested for first enrollment.",
      "properties": {
        "clientInstanceId": {
          "description": "Consumer-minted installed-instance label, never authority by itself.",
          "type": "string"
        },
        "manifestDigest": {
          "description": "Base64url digest of the consumer's closed product manifest.",
          "type": "string"
        },
        "publicKey": {
          "description": "Base64url Ed25519 verifying key.",
          "type": "string"
        },
        "requestedRoots": {
          "description": "Exact requested project paths. Approval canonicalizes and may only narrow them.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "requestedScopes": {
          "description": "Exact requested scopes. Approval may only narrow them.",
          "items": {
            "$ref": "#/$defs/AppScope"
          },
          "type": "array"
        }
      },
      "required": [
        "clientInstanceId",
        "publicKey",
        "manifestDigest",
        "requestedScopes",
        "requestedRoots"
      ],
      "type": "object"
    },
    "EnrollmentReceipt": {
      "additionalProperties": false,
      "description": "Enrollment was recorded without revealing Runtime inventory.",
      "properties": {
        "expiresAtMs": {
          "description": "When the request expires in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "pendingId": {
          "$ref": "#/$defs/PendingEnrollmentId",
          "description": "Opaque local decision identity."
        }
      },
      "required": [
        "pendingId",
        "expiresAtMs"
      ],
      "type": "object"
    },
    "ErrorResponse": {
      "additionalProperties": false,
      "description": "Failed JSON-RPC response.",
      "properties": {
        "error": {
          "$ref": "#/$defs/RuntimeError",
          "description": "Stable public failure."
        },
        "id": {
          "$ref": "#/$defs/JsonRpcId",
          "description": "Request identifier copied unchanged."
        },
        "jsonrpc": {
          "description": "Must be exactly `2.0`.",
          "type": "string"
        }
      },
      "required": [
        "jsonrpc",
        "id",
        "error"
      ],
      "type": "object"
    },
    "EventCursor": {
      "additionalProperties": false,
      "description": "Public reconnect cursor over the existing bounded Runtime replay ring.",
      "properties": {
        "epoch": {
          "description": "Live process attachment generation.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "seq": {
          "description": "Dense next event sequence.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "stream": {
          "description": "Runtime-minted stream identity.",
          "type": "string"
        }
      },
      "required": [
        "stream",
        "epoch",
        "seq"
      ],
      "type": "object"
    },
    "EventGap": {
      "additionalProperties": false,
      "description": "Explicit replay gap when the requested cursor fell outside the bounded ring.",
      "properties": {
        "liveAt": {
          "$ref": "#/$defs/EventCursor",
          "description": "Oldest boundary Runtime can serve now."
        },
        "requested": {
          "$ref": "#/$defs/EventCursor",
          "description": "Cursor the consumer requested."
        }
      },
      "required": [
        "requested",
        "liveAt"
      ],
      "type": "object"
    },
    "ForgetSessionParams": {
      "additionalProperties": false,
      "description": "Forget one cold Runtime pointer after an exact local close confirmation.",
      "properties": {
        "expectedSessionGeneration": {
          "description": "Lifecycle generation visible when the caller requested removal.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity retained across the local confirmation."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session pointer to forget."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "expectedSessionGeneration"
      ],
      "type": "object"
    },
    "GetProviderCapabilitiesParams": {
      "additionalProperties": false,
      "description": "Select one provider for explicit capability discovery.",
      "properties": {
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by `providers/list`."
        }
      },
      "required": [
        "providerId"
      ],
      "type": "object"
    },
    "GetSessionParams": {
      "additionalProperties": false,
      "description": "Select one exact Runtime-managed session.",
      "properties": {
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Stable opaque identity returned by the managed session catalogue."
        }
      },
      "required": [
        "sessionId"
      ],
      "type": "object"
    },
    "InitializeParams": {
      "description": "Initialization is negotiation only. Inventory is a separate authorized request.",
      "properties": {
        "authentication": {
          "anyOf": [
            {
              "$ref": "#/$defs/IntegrationAuthentication"
            },
            {
              "type": "null"
            }
          ],
          "description": "Approved integration proof when reconnecting. Omit only to enroll."
        },
        "client": {
          "$ref": "#/$defs/ClientInfo",
          "description": "Safe client metadata."
        },
        "clientCapabilities": {
          "$ref": "#/$defs/ClientCapabilities",
          "default": {
            "opaqueEventExtensions": false
          },
          "description": "Closed client capability map."
        },
        "supportedRevisions": {
          "description": "Every finalized revision the client implements.",
          "items": {
            "$ref": "#/$defs/ProtocolRevision"
          },
          "type": "array"
        }
      },
      "required": [
        "supportedRevisions",
        "client"
      ],
      "type": "object"
    },
    "InitializeResult": {
      "description": "Successful initialization before integration authorization.",
      "properties": {
        "grant": {
          "anyOf": [
            {
              "$ref": "#/$defs/IntegrationGrant"
            },
            {
              "type": "null"
            }
          ],
          "description": "Current authority when initialization authenticated an approved integration."
        },
        "limits": {
          "$ref": "#/$defs/RuntimeLimits",
          "description": "Numeric admission limits."
        },
        "runtime": {
          "$ref": "#/$defs/RuntimeInstance",
          "description": "Running instance proof."
        },
        "selectedRevision": {
          "$ref": "#/$defs/ProtocolRevision",
          "description": "Newest common finalized revision."
        },
        "serverCapabilities": {
          "$ref": "#/$defs/RuntimeCapabilities",
          "description": "Implemented product capabilities. Scope checks still apply."
        }
      },
      "required": [
        "selectedRevision",
        "runtime",
        "serverCapabilities",
        "limits"
      ],
      "type": "object"
    },
    "InstallationObservation": {
      "additionalProperties": false,
      "description": "Structural provider installation evidence with no credentials or raw output.",
      "properties": {
        "state": {
          "$ref": "#/$defs/InstallationState",
          "description": "Current usable state."
        },
        "version": {
          "description": "Provider-owned version text when the bounded probe supplied it.",
          "type": [
            "string",
            "null"
          ]
        },
        "why": {
          "description": "Safe structural reason when the provider is not usable.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "state"
      ],
      "type": "object"
    },
    "InstallationState": {
      "description": "Whether an installed provider can currently be used.",
      "oneOf": [
        {
          "const": "usable",
          "description": "The registered discovery ladder found a usable program.",
          "type": "string"
        },
        {
          "const": "missing",
          "description": "No program matching the registered discovery ladder was found.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "A program was found but its supported surface could not be confirmed.",
          "type": "string"
        }
      ]
    },
    "IntegrationAuthentication": {
      "additionalProperties": false,
      "description": "Authentication for an approved integration during initialization.",
      "properties": {
        "grantGeneration": {
          "description": "Grant generation expected by the client.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "integrationId": {
          "$ref": "#/$defs/IntegrationId",
          "description": "Approved integration."
        },
        "keyGeneration": {
          "description": "Public key generation expected by the client.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "signature": {
          "description": "Base64url Ed25519 signature over the canonical initialization payload.",
          "type": "string"
        }
      },
      "required": [
        "integrationId",
        "keyGeneration",
        "grantGeneration",
        "signature"
      ],
      "type": "object"
    },
    "IntegrationGrant": {
      "additionalProperties": false,
      "description": "The caller's current approved authority.",
      "properties": {
        "grantGeneration": {
          "description": "Generation invalidating already parsed requests after grant changes.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "integrationId": {
          "$ref": "#/$defs/IntegrationId",
          "description": "Stable integration identity."
        },
        "keyGeneration": {
          "description": "Key generation required for signatures.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "roots": {
          "description": "Canonical approved project paths.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "scopes": {
          "description": "Exact current app scopes.",
          "items": {
            "$ref": "#/$defs/AppScope"
          },
          "type": "array"
        }
      },
      "required": [
        "integrationId",
        "scopes",
        "roots",
        "keyGeneration",
        "grantGeneration"
      ],
      "type": "object"
    },
    "IntegrationId": {
      "description": "An operator-approved local integration instance.",
      "type": "string"
    },
    "JsonRpcId": {
      "anyOf": [
        {
          "description": "Connection-local numeric identifier.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        {
          "description": "Caller-owned opaque string identifier.",
          "type": "string"
        }
      ],
      "description": "A JSON-RPC request identifier."
    },
    "JsonRpcNotification": {
      "additionalProperties": false,
      "description": "One JSON-RPC notification with no response ID.",
      "properties": {
        "jsonrpc": {
          "description": "Must be exactly `2.0`.",
          "type": "string"
        },
        "method": {
          "description": "Stable public method name.",
          "type": "string"
        },
        "params": {
          "default": null,
          "description": "Method parameters."
        }
      },
      "required": [
        "jsonrpc",
        "method"
      ],
      "type": "object"
    },
    "JsonRpcRequest": {
      "additionalProperties": false,
      "description": "One JSON-RPC request. Method parameter schemas remain closed at their typed decode boundary.",
      "properties": {
        "id": {
          "$ref": "#/$defs/JsonRpcId",
          "description": "Connection-local response correlation."
        },
        "jsonrpc": {
          "description": "Must be exactly `2.0`.",
          "type": "string"
        },
        "method": {
          "description": "Stable public method name.",
          "type": "string"
        },
        "params": {
          "default": null,
          "description": "Method parameters."
        }
      },
      "required": [
        "jsonrpc",
        "id",
        "method"
      ],
      "type": "object"
    },
    "JsonRpcResponse": {
      "anyOf": [
        {
          "$ref": "#/$defs/SuccessResponse",
          "description": "A typed result encoded in the envelope."
        },
        {
          "$ref": "#/$defs/ErrorResponse",
          "description": "A stable machine failure."
        }
      ],
      "description": "A response is exactly one success or one failure."
    },
    "LaggedNotification": {
      "additionalProperties": false,
      "description": "A slow subscriber was retired at an exact missing boundary.",
      "properties": {
        "nextExpected": {
          "$ref": "#/$defs/EventCursor",
          "description": "First event the subscriber did not receive."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact watched session."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "sessionId",
        "nextExpected"
      ],
      "type": "object"
    },
    "LifecycleState": {
      "description": "Structural Runtime supervision state without conversation meaning.",
      "oneOf": [
        {
          "const": "hotIdle",
          "description": "A provider process is present and not currently in a turn.",
          "type": "string"
        },
        {
          "const": "hotRunning",
          "description": "A provider process is in a turn.",
          "type": "string"
        },
        {
          "const": "cold",
          "description": "Runtime has metadata but no provider process.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "The last provider operation failed structurally.",
          "type": "string"
        }
      ]
    },
    "ListModelsParams": {
      "additionalProperties": false,
      "description": "Select one provider for explicit, potentially slow model discovery.",
      "properties": {
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by `providers/list`."
        }
      },
      "required": [
        "providerId"
      ],
      "type": "object"
    },
    "ListNativeSessionsParams": {
      "additionalProperties": false,
      "description": "Select one provider, and either one approved folder or the whole machine, for native discovery.",
      "properties": {
        "cursor": {
          "description": "Runtime-wrapped opaque cursor returned by the preceding page.",
          "type": [
            "string",
            "null"
          ]
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by `providers/list`."
        },
        "root": {
          "description": "Exact canonical root already present in the authenticated integration grant, or absent to\nask for every conversation the provider will name.\n\nAbsence is answered only on the owner-only local endpoint, and only by a provider whose own\nsurface enumerates without a folder filter. A caller that omits it and gets a refusal knows\nto ask per folder; a caller that supplies it gets exactly the folder it named, as before.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "providerId"
      ],
      "type": "object"
    },
    "ListPendingApprovalsParams": {
      "additionalProperties": false,
      "description": "Current control lease required to inspect pending approvals for one session.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque current lease identity.",
          "type": "string"
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact controlled Runtime session."
        }
      },
      "required": [
        "sessionId",
        "leaseId",
        "leaseGeneration"
      ],
      "type": "object"
    },
    "ListTerminalsParams": {
      "additionalProperties": false,
      "description": "Read the visible terminal index in the connected Runtime generation.",
      "type": "object"
    },
    "ManagedSessionList": {
      "additionalProperties": false,
      "description": "A bounded Runtime-managed session snapshot.",
      "properties": {
        "sessions": {
          "description": "Sessions already known to Runtime.",
          "items": {
            "$ref": "#/$defs/SessionDescriptor"
          },
          "type": "array"
        },
        "warnings": {
          "description": "Safe structural omissions that do not identify an unauthorized project.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "sessions",
        "warnings"
      ],
      "type": "object"
    },
    "MutationRequestId": {
      "description": "A caller-minted UUIDv7 identifying one state-changing request.",
      "type": "string"
    },
    "NativeActivity": {
      "additionalProperties": false,
      "description": "The conversations of one provider that were written inside the activity window.",
      "properties": {
        "active": {
          "description": "Native identities written inside the window, in no particular order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "The provider asked about."
        }
      },
      "required": [
        "providerId",
        "active"
      ],
      "type": "object"
    },
    "NativeActivityParams": {
      "additionalProperties": false,
      "description": "Ask which of one provider's conversations have a model answering right now.\n\nSeparate from the catalogue because it is asked often and the catalogue is not cheap: on the machine this\nwas measured, naming every stored conversation costs 121 ms. How a provider knows is its own business\n(Claude Code publishes a roster of its own running processes and what each one is doing); what the answer\nmeans is the same for every provider, and it is how the panel shows a turn running in a conversation\nRuntrol did not start.",
      "properties": {
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by `providers/list`."
        }
      },
      "required": [
        "providerId"
      ],
      "type": "object"
    },
    "NativeResumeCapability": {
      "description": "Whether an officially listed session can be resumed through the same provider driver.",
      "oneOf": [
        {
          "const": "available",
          "description": "An official resume operation is available.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "The provider explicitly does not advertise resume support.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The official discovery surface cannot establish resume support.",
          "type": "string"
        }
      ]
    },
    "NativeSessionCatalogue": {
      "additionalProperties": false,
      "description": "One bounded provider-native session page after authorization and filtering.",
      "properties": {
        "coverage": {
          "$ref": "#/$defs/CatalogueCoverage",
          "description": "Honest coverage after Runtime path filtering."
        },
        "nextCursor": {
          "description": "Runtime-wrapped opaque cursor for the next page.",
          "type": [
            "string",
            "null"
          ]
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Exact provider context."
        },
        "sessions": {
          "description": "Root-authorized entries in provider order.",
          "items": {
            "$ref": "#/$defs/NativeSessionDescriptor"
          },
          "type": "array"
        }
      },
      "required": [
        "providerId",
        "coverage",
        "sessions"
      ],
      "type": "object"
    },
    "NativeSessionDescriptor": {
      "additionalProperties": false,
      "description": "One root-authorized official provider-native session.",
      "properties": {
        "additionalDirectories": {
          "description": "Canonical authorized additional roots in provider order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "adoptionToken": {
          "description": "Short-lived Runtime proof required to adopt this exact authorized observation.",
          "type": [
            "string",
            "null"
          ]
        },
        "alreadyManagedAs": {
          "anyOf": [
            {
              "$ref": "#/$defs/RuntimeSessionId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Existing Runtime pointer matched only by provider and native identity."
        },
        "cwd": {
          "description": "Canonical authorized primary working directory.",
          "type": "string"
        },
        "nativeSessionId": {
          "description": "Provider-owned opaque identity returned unchanged for adoption.",
          "type": "string"
        },
        "resume": {
          "$ref": "#/$defs/NativeResumeCapability",
          "description": "Officially discovered resume capability."
        },
        "title": {
          "description": "Provider-owned presentation title, if officially supplied.",
          "type": [
            "string",
            "null"
          ]
        },
        "updatedAt": {
          "description": "Provider-owned official timestamp representation, if supplied.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "nativeSessionId",
        "cwd",
        "additionalDirectories",
        "resume"
      ],
      "type": "object"
    },
    "PendingApproval": {
      "additionalProperties": false,
      "description": "One provider-neutral approval request retained by the live driver.",
      "properties": {
        "approvalId": {
          "description": "Runtrol-owned approval identity.",
          "type": "string"
        },
        "expiresAtMs": {
          "description": "Provider request expiry in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "kind": {
          "$ref": "#/$defs/RuntimeApprovalKind",
          "description": "Structural provider request class."
        },
        "options": {
          "description": "Every provider-offered option without silently hiding unavailable choices.",
          "items": {
            "$ref": "#/$defs/RuntimeApprovalOption"
          },
          "type": "array"
        },
        "risk": {
          "$ref": "#/$defs/RuntimeApprovalRisk",
          "description": "Risk computed from the held provider request."
        },
        "subject": {
          "description": "Provider-normalized subject that must be presented for meaningful consent."
        },
        "subjectDigest": {
          "description": "Exact subject digest required in the response.",
          "items": {
            "format": "uint8",
            "maximum": 255,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 32,
          "minItems": 32,
          "type": "array"
        },
        "subjectIncomplete": {
          "description": "Whether Runtime could not bind the complete subject.",
          "type": "boolean"
        }
      },
      "required": [
        "approvalId",
        "kind",
        "risk",
        "options",
        "subject",
        "subjectIncomplete",
        "subjectDigest",
        "expiresAtMs"
      ],
      "type": "object"
    },
    "PendingApprovalList": {
      "additionalProperties": false,
      "description": "Every provider approval still pending for the exact controlled session.",
      "properties": {
        "approvals": {
          "description": "Bounded pending requests in provider order.",
          "items": {
            "$ref": "#/$defs/PendingApproval"
          },
          "type": "array"
        }
      },
      "required": [
        "approvals"
      ],
      "type": "object"
    },
    "PendingEnrollmentId": {
      "description": "An opaque pending local enrollment decision.",
      "type": "string"
    },
    "ProtocolRevision": {
      "description": "A finalized public Runtime contract date.",
      "type": "string"
    },
    "ProviderAccount": {
      "additionalProperties": false,
      "description": "One service's account report, structured fields only.\n\nLimit windows a service reports on request (outside any turn) do not travel here: they land in the\nusage list beside the windows a turn reports, so a surface reads one gauge per service.",
      "properties": {
        "checkedAtMs": {
          "description": "When the service answered, in unix milliseconds, which is how a surface says how stale it is.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "limitsAbsent": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderLimitsAbsent"
            },
            {
              "type": "null"
            }
          ],
          "description": "Why this signed-in account has no limit numbers, when it has none.\n\nTwo silences that a surface has to tell apart: one is the service answering that this account is\nmetered somewhere the operator cannot see, and the other is a question that did not come back. They\nwant different words on the row, and only the second is anybody's to retry."
        },
        "method": {
          "description": "How the operator is signed in, as the service names it, when it says.",
          "type": [
            "string",
            "null"
          ]
        },
        "plan": {
          "description": "The plan token exactly as the service wrote it, when it names one.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "$ref": "#/$defs/ProviderAccountStatus",
          "description": "Signed in, signed out, or nothing to ask."
        },
        "why": {
          "description": "Why nothing can be asked, in the service's own terms, for an unpublished status.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "status",
        "checkedAtMs"
      ],
      "type": "object"
    },
    "ProviderAccountStatus": {
      "description": "Whether the operator is signed in to one service, by that service's own word.",
      "oneOf": [
        {
          "const": "signedIn",
          "description": "The service said the operator is signed in.",
          "type": "string"
        },
        {
          "const": "signedOut",
          "description": "The service said nobody is signed in.",
          "type": "string"
        },
        {
          "const": "unpublished",
          "description": "The service publishes no way to ask.",
          "type": "string"
        }
      ]
    },
    "ProviderCapabilityAvailability": {
      "description": "Whether one structural provider operation is usable in the observed installation.",
      "oneOf": [
        {
          "const": "available",
          "description": "The exact prepared driver reports an available official surface.",
          "type": "string"
        },
        {
          "const": "unsupported",
          "description": "No registered official surface exists.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Provider negotiation is required before Runtime can answer.",
          "type": "string"
        }
      ]
    },
    "ProviderCapabilityObservation": {
      "additionalProperties": false,
      "description": "One sanitized structural capability observation.",
      "properties": {
        "availability": {
          "$ref": "#/$defs/ProviderCapabilityAvailability",
          "description": "Current availability."
        },
        "provenance": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderCapabilityProvenance"
            },
            {
              "type": "null"
            }
          ],
          "description": "Source of an available answer."
        },
        "why": {
          "description": "Stable explanation for an unsupported or unknown answer.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "availability"
      ],
      "type": "object"
    },
    "ProviderCapabilityProvenance": {
      "description": "Provenance of an available structural capability.",
      "oneOf": [
        {
          "const": "officialProtocol",
          "description": "A provider-neutral protocol negotiation or stable protocol contract.",
          "type": "string"
        },
        {
          "const": "officialCli",
          "description": "A provider-owned command, flag parser, or structured stream.",
          "type": "string"
        },
        {
          "const": "driverContract",
          "description": "Runtime's registered driver lifecycle contract.",
          "type": "string"
        }
      ]
    },
    "ProviderDescriptor": {
      "additionalProperties": false,
      "description": "One provider in the fast inventory.",
      "properties": {
        "account": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderAccount"
            },
            {
              "type": "null"
            }
          ],
          "description": "Where the operator's account with this service stands, by the service's own status surface.\n\nAbsent until Runtime has asked the service once; a surface then says \"not checked yet\" rather than\ninventing a green light. Present for every installed service afterwards, including one whose\nservice publishes no way to ask, which the report says in so many words."
        },
        "displayName": {
          "description": "Provider or manifest supplied label for presentation only.",
          "type": "string"
        },
        "help": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderHelp"
            },
            {
              "type": "null"
            }
          ],
          "description": "This service's own commands for making itself usable, when it declares any.\n\nAbsent rather than empty when nothing is declared, so a client shows nothing instead of an action\nthat leads nowhere."
        },
        "icon": {
          "description": "The editor glyph that stands for this service, for presentation only.\n\nA name, never artwork. Editors carry marks for several of these services already, and naming one is how a\nsurface shows a conversation's service without anybody shipping a trademark. Absent when the manifest\ndeclared none, and a surface then shows whatever it shows for a service it does not recognise.",
          "type": [
            "string",
            "null"
          ]
        },
        "installation": {
          "$ref": "#/$defs/InstallationObservation",
          "description": "Fast cached installation evidence. Listing does not start the provider."
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque selection value."
        },
        "switchableModes": {
          "description": "The mode tokens this service accepts a runtrol switch to, when its vocabulary is a manifest fact.\n\nEmpty for a protocol that announces its modes per session; the session's own announcement is then\nthe list a surface offers. The daemon enforces the same boundary on `sessions/setMode`, so this list\nis presentation, never authority.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "providerId",
        "displayName",
        "installation"
      ],
      "type": "object"
    },
    "ProviderHelp": {
      "additionalProperties": false,
      "description": "A coding service's own commands for making itself usable, ready to show a person.\n\n# Why Runtime sends finished command lines\n\nA declaration names arguments; only Runtime knows which executable actually resolved. A client that\njoined the two would be a second place that decides what runs, and it would be wrong on exactly the\nmachine where a second candidate was the installed one.\n\n# What a client may do with these\n\nOffer them. Nothing else. Runtime does not run them and neither should a client: fetching and\nexecuting on a person's behalf is the capability this product refused from the start, and an install\nbutton that runs is that capability with a friendly label. The operator reads the line and decides.\n\nEvery string is validated at the declaration boundary to contain no character a shell could read as a\nseparator, so a client can present one without quoting it into something else.",
      "properties": {
        "diagnose": {
          "description": "The command that makes this service diagnose its own installation, when it declares one.",
          "type": [
            "string",
            "null"
          ]
        },
        "install": {
          "description": "The command that installs this service, for when no executable exists to ask.",
          "type": [
            "string",
            "null"
          ]
        },
        "signIn": {
          "description": "The command that signs in to this service, when it declares one.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ProviderId": {
      "description": "An opaque provider identity discovered by Runtime.",
      "type": "string"
    },
    "ProviderLimitsAbsent": {
      "additionalProperties": false,
      "description": "Why a signed-in account shows no limit numbers.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/ProviderLimitsAbsentKind",
          "description": "Which of the two silences this is."
        },
        "why": {
          "description": "The reason, in the service's own words where it gave one.",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "why"
      ],
      "type": "object"
    },
    "ProviderLimitsAbsentKind": {
      "description": "The two silences, told apart.",
      "oneOf": [
        {
          "const": "unread",
          "description": "The surface was asked and the answer could not be read. Runtime asks again soon.",
          "type": "string"
        },
        {
          "const": "unmetered",
          "description": "The service answered and this account has no numbers of its own. Asking again changes nothing.",
          "type": "string"
        }
      ]
    },
    "ProviderList": {
      "additionalProperties": false,
      "description": "A bounded provider inventory snapshot.",
      "properties": {
        "providers": {
          "description": "Providers in registry order, including unavailable entries.",
          "items": {
            "$ref": "#/$defs/ProviderDescriptor"
          },
          "type": "array"
        }
      },
      "required": [
        "providers"
      ],
      "type": "object"
    },
    "ProviderUsageCost": {
      "additionalProperties": false,
      "description": "Money a provider reported spending, exactly as it stated it.",
      "properties": {
        "amount": {
          "description": "How much.",
          "format": "double",
          "type": "number"
        },
        "currency": {
          "description": "The currency as the provider wrote it, never converted.",
          "type": "string"
        }
      },
      "required": [
        "amount",
        "currency"
      ],
      "type": "object"
    },
    "ProviderUsageGauge": {
      "additionalProperties": false,
      "description": "One provider's most recent limit report.",
      "properties": {
        "atMs": {
          "description": "When the report arrived, in unix milliseconds, which is how a surface says how stale it is.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "cost": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderUsageCost"
            },
            {
              "type": "null"
            }
          ],
          "description": "The latest running spend the provider stated, when it states one.\n\nAbsent for a provider that reports only limits. The newest report wins, so this is the most recent\nturn's cost as the provider gave it, never a total runtrol summed."
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque selection value."
        },
        "reached": {
          "description": "A limit is blocking right now, by the provider's own word.",
          "type": "boolean"
        },
        "tokensToday": {
          "description": "Tokens the account spent today by the provider's own daily count, when it publishes one.\n\nRead on request from the provider's own usage surface (the manifest's account protocol), never summed by\nRuntime. Absent for a provider that publishes limits only.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "windows": {
          "description": "Every limit window the provider described, shortest first.\n\nA list, because a plan is not two windows: measured, one service publishes a five-hour window, a\nwhole-account week and a week scoped to one model, and another publishes a short and a long window for\neach metered model. A surface draws them all rather than being handed the two a driver chose.",
          "items": {
            "$ref": "#/$defs/ProviderUsageWindow"
          },
          "type": "array"
        }
      },
      "required": [
        "providerId",
        "reached",
        "atMs"
      ],
      "type": "object"
    },
    "ProviderUsageList": {
      "additionalProperties": false,
      "description": "Where each account stands against its limits, by each provider's own latest report.\n\nStructured fields only, never the provider's verbatim payload: that payload rides the session event stream\nunder session-output authority, and this list answers under provider authority. A gauge absent from the list\nmeans that provider has not reported since the Runtime started, which is different from a limit not existing,\nand a surface says \"no report yet\" rather than inventing a green light.",
      "properties": {
        "providers": {
          "description": "Latest reports in provider order, one per provider that has reported.",
          "items": {
            "$ref": "#/$defs/ProviderUsageGauge"
          },
          "type": "array"
        }
      },
      "required": [
        "providers"
      ],
      "type": "object"
    },
    "ProviderUsageWindow": {
      "additionalProperties": false,
      "description": "One rate limit window, as far as the provider described it.",
      "properties": {
        "governing": {
          "description": "The provider says this window is the one governing right now.",
          "type": "boolean"
        },
        "id": {
          "description": "Stable identity within one provider's report, as that provider names the window.\n\nA surface keys its row on this, so a window keeps its place when a later report adds a sibling.",
          "type": "string"
        },
        "label": {
          "description": "What the provider calls this window for a person to read, when it names one.\n\nThe provider's own label and nothing composed here. Absent when it named none, and a surface then\nsays the window's length instead of inventing a name.",
          "type": [
            "string",
            "null"
          ]
        },
        "resetsAtMs": {
          "description": "When it resets, in unix milliseconds, when the provider says.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "scope": {
          "description": "What this limit is scoped to, when the provider scopes it (one model, one surface).",
          "type": [
            "string",
            "null"
          ]
        },
        "usedPercent": {
          "description": "How much of the window is used, as a percentage, when the provider reports one.\n\nOptional because it is not universal: measured, one provider reports which window governs and when it\nresets while saying nothing about how full it is.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "windowMinutes": {
          "description": "How long the window is, in minutes, when the provider says.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "ProviderWatchEndReason": {
      "description": "Why a provider inventory subscription ended.",
      "oneOf": [
        {
          "const": "integrationRevoked",
          "description": "The integration grant was revoked.",
          "type": "string"
        },
        {
          "const": "authorityChanged",
          "description": "Scope or integration generations changed and require authenticated reconnect.",
          "type": "string"
        },
        {
          "const": "runtimeUnavailable",
          "description": "The Runtime provider inventory publisher stopped.",
          "type": "string"
        }
      ]
    },
    "ProviderWatchEndedNotification": {
      "additionalProperties": false,
      "description": "Final typed reason for retiring a provider inventory subscription.",
      "properties": {
        "reason": {
          "$ref": "#/$defs/ProviderWatchEndReason",
          "description": "Structural terminal reason."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "reason"
      ],
      "type": "object"
    },
    "ProvidersChangedNotification": {
      "additionalProperties": false,
      "description": "A changed complete provider inventory snapshot.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/ProviderList",
          "description": "New complete provider snapshot."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "ProvidersUsageChangedNotification": {
      "additionalProperties": false,
      "description": "A changed account usage snapshot, delivered on the provider inventory subscription.\n\nUsage moves with every turn and every probe; a subscriber draws it the moment it changes instead of\nasking `providers/usage` on a clock. Sent once right after the subscription is installed, so a\nsubscriber never needs the request at all.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/ProviderUsageList",
          "description": "New complete usage snapshot."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "PublicInputBlock": {
      "description": "One typed piece of caller input, transported without rewriting.\n\nIts own method (`sessions/submitBlocks`) rather than a widened `sessions/submitInput`, because one\nDTO holding both a plain string and a block list would carry two representations of the same\nprompt and two validation regimes for one method.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Text, exactly as written.",
          "properties": {
            "text": {
              "description": "The caller's text, never rewritten.",
              "type": "string"
            },
            "type": {
              "const": "text",
              "type": "string"
            }
          },
          "required": [
            "type",
            "text"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "One attached image, as bytes.",
          "properties": {
            "base64Data": {
              "description": "The image bytes, base64 encoded and never inspected.",
              "type": "string"
            },
            "mediaType": {
              "description": "The IANA media type the attachment declared, e.g. `image/png`.",
              "type": "string"
            },
            "type": {
              "const": "image",
              "type": "string"
            }
          },
          "required": [
            "type",
            "mediaType",
            "base64Data"
          ],
          "type": "object"
        }
      ]
    },
    "RequestEnrollmentParams": {
      "additionalProperties": false,
      "description": "Prove possession of the key attached to a new enrollment.",
      "properties": {
        "manifest": {
          "$ref": "#/$defs/EnrollmentManifest",
          "description": "Closed proposal."
        },
        "signature": {
          "description": "Base64url Ed25519 signature over the canonical enrollment payload.",
          "type": "string"
        }
      },
      "required": [
        "manifest",
        "signature"
      ],
      "type": "object"
    },
    "RespondApprovalParams": {
      "additionalProperties": false,
      "description": "Answer one exact provider approval under the current control lease.",
      "properties": {
        "approvalId": {
          "description": "Exact pending approval identity.",
          "type": "string"
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque current lease identity.",
          "type": "string"
        },
        "optionId": {
          "description": "Exact provider-offered option identity.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact controlled Runtime session."
        },
        "subjectDigest": {
          "description": "Digest returned by `approvals/listPending`.",
          "items": {
            "format": "uint8",
            "maximum": 255,
            "minimum": 0,
            "type": "integer"
          },
          "maxItems": 32,
          "minItems": 32,
          "type": "array"
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration",
        "approvalId",
        "optionId",
        "subjectDigest"
      ],
      "type": "object"
    },
    "ResumeSessionParams": {
      "additionalProperties": false,
      "description": "Heat one existing Runtime-managed cold session.",
      "properties": {
        "access": {
          "$ref": "#/$defs/SessionWorkspaceAccess",
          "description": "Writer collision posture for the working tree."
        },
        "expectedLifecycle": {
          "$ref": "#/$defs/LifecycleState",
          "description": "Lifecycle visible when the caller chose to resume."
        },
        "expectedSessionGeneration": {
          "description": "Lifecycle generation visible when the caller chose to resume.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "model": {
          "description": "Exact opaque model choice previously returned by Runtime, or the session's own setting when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "reasoningEffort": {
          "description": "Exact opaque reasoning choice returned for the selected model, or the session's own setting when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        },
        "workspace": {
          "description": "Exact current managed workspace, reauthorized before provider I/O.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "expectedLifecycle",
        "expectedSessionGeneration",
        "workspace",
        "access"
      ],
      "type": "object"
    },
    "RotateIntegrationKeyParams": {
      "additionalProperties": false,
      "description": "Replace one approved integration key after an exact local confirmation.",
      "properties": {
        "expectedKeyGeneration": {
          "description": "Key generation observed before the rotation began.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "newKeyProof": {
          "description": "Base64url signature by the new key over the canonical rotation payload.",
          "type": "string"
        },
        "newPublicKey": {
          "description": "Base64url Ed25519 verification key that will replace the current key.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-owned idempotency identity retained across retries."
        }
      },
      "required": [
        "requestId",
        "expectedKeyGeneration",
        "newPublicKey",
        "newKeyProof"
      ],
      "type": "object"
    },
    "RuntimeApprovalKind": {
      "description": "Structural provider approval class.",
      "oneOf": [
        {
          "const": "command",
          "description": "Run a command.",
          "type": "string"
        },
        {
          "const": "fileChange",
          "description": "Change files.",
          "type": "string"
        },
        {
          "const": "permissions",
          "description": "Widen permissions.",
          "type": "string"
        },
        {
          "const": "elicitation",
          "description": "Answer a provider question.",
          "type": "string"
        },
        {
          "const": "network",
          "description": "Reach the network.",
          "type": "string"
        },
        {
          "const": "other",
          "description": "A structural class unknown to this revision.",
          "type": "string"
        }
      ]
    },
    "RuntimeApprovalOption": {
      "additionalProperties": false,
      "description": "One provider-offered approval option and its current availability.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/RuntimeApprovalOptionKind",
          "description": "Structural effect of choosing the option."
        },
        "label": {
          "description": "Provider-owned presentation label transported verbatim.",
          "type": "string"
        },
        "optionId": {
          "description": "Runtrol-owned option identity within this approval.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "unavailable": {
          "description": "Safe Runtime-owned reason the current integration cannot choose it.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "optionId",
        "label",
        "kind"
      ],
      "type": "object"
    },
    "RuntimeApprovalOptionKind": {
      "description": "Structural effect of one provider-offered option.",
      "oneOf": [
        {
          "const": "allowOnce",
          "description": "Allow only this action.",
          "type": "string"
        },
        {
          "const": "allowAlways",
          "description": "Create a standing allowance.",
          "type": "string"
        },
        {
          "const": "rejectOnce",
          "description": "Reject only this action.",
          "type": "string"
        },
        {
          "const": "rejectAlways",
          "description": "Create a standing rejection.",
          "type": "string"
        }
      ]
    },
    "RuntimeApprovalRisk": {
      "description": "Authority class derived from the pending request and selected option.",
      "oneOf": [
        {
          "const": "low",
          "description": "One action with no standing policy change.",
          "type": "string"
        },
        {
          "const": "high",
          "description": "Code execution, destructive action, or standing policy change.",
          "type": "string"
        }
      ]
    },
    "RuntimeCapabilities": {
      "description": "Public product capabilities for the selected revision.",
      "properties": {
        "integrationEnrollment": {
          "description": "Local integration enrollment is implemented.",
          "type": "boolean"
        },
        "managedSessionList": {
          "description": "Fast managed session snapshot is implemented.",
          "type": "boolean"
        },
        "modelDiscovery": {
          "description": "Explicit provider model discovery is implemented.",
          "type": "boolean"
        },
        "nativeSessionCatalogue": {
          "description": "Root-scoped official provider-native session discovery is implemented.",
          "type": "boolean"
        },
        "providerInventory": {
          "description": "Fast provider inventory method is implemented.",
          "type": "boolean"
        },
        "sessionControl": {
          "description": "Renewable single-writer session control is implemented.",
          "type": "boolean"
        },
        "sessionEvents": {
          "description": "Bounded replay followed by live normalized events is implemented.",
          "type": "boolean"
        },
        "terminalSurface": {
          "default": false,
          "description": "Provider-faithful public terminal sessions are implemented for this generation.",
          "type": "boolean"
        }
      },
      "required": [
        "integrationEnrollment",
        "providerInventory",
        "managedSessionList",
        "modelDiscovery",
        "nativeSessionCatalogue",
        "sessionControl",
        "sessionEvents"
      ],
      "type": "object"
    },
    "RuntimeEndpointKind": {
      "description": "Local transport kind named by the platform locator.",
      "oneOf": [
        {
          "const": "namedPipe",
          "description": "Windows current-user named pipe.",
          "type": "string"
        },
        {
          "const": "unixSocket",
          "description": "Unix owner-only domain socket.",
          "type": "string"
        }
      ]
    },
    "RuntimeError": {
      "additionalProperties": false,
      "description": "A public failure with stable machine fields and bounded safe text.",
      "properties": {
        "code": {
          "$ref": "#/$defs/RuntimeErrorKind",
          "description": "Stable branch key."
        },
        "correlationId": {
          "description": "Opaque correlation identifier with no content-derived data.",
          "type": "string"
        },
        "message": {
          "description": "Safe presentation text. Clients do not parse it.",
          "type": "string"
        },
        "operatorAction": {
          "description": "Optional stable local action name.",
          "type": [
            "string",
            "null"
          ]
        },
        "retryable": {
          "description": "Whether repeating later may succeed without changing parameters.",
          "type": "boolean"
        }
      },
      "required": [
        "code",
        "message",
        "retryable",
        "correlationId"
      ],
      "type": "object"
    },
    "RuntimeErrorKind": {
      "description": "A stable public failure category. Clients never branch on prose.",
      "oneOf": [
        {
          "const": "runtimeNotInstalled",
          "description": "No verified Runtime installation is present.",
          "type": "string"
        },
        {
          "const": "runtimeUnavailable",
          "description": "The Runtime cannot currently be reached or trusted.",
          "type": "string"
        },
        {
          "const": "protocolIncompatible",
          "description": "The client and Runtime share no finalized revision.",
          "type": "string"
        },
        {
          "const": "notInitialized",
          "description": "A method was sent before initialization completed.",
          "type": "string"
        },
        {
          "const": "unauthenticated",
          "description": "The connection has no enrolled integration identity.",
          "type": "string"
        },
        {
          "const": "enrollmentPending",
          "description": "The integration is waiting for local approval.",
          "type": "string"
        },
        {
          "const": "enrollmentDenied",
          "description": "The operator denied enrollment.",
          "type": "string"
        },
        {
          "const": "integrationRevoked",
          "description": "The integration grant was revoked.",
          "type": "string"
        },
        {
          "const": "scopeDenied",
          "description": "The integration grant lacks the method scope.",
          "type": "string"
        },
        {
          "const": "presenceRequired",
          "description": "A private local action is required.",
          "type": "string"
        },
        {
          "const": "rootDenied",
          "description": "The target is outside the approved project roots.",
          "type": "string"
        },
        {
          "const": "providerUnavailable",
          "description": "The provider is not currently usable.",
          "type": "string"
        },
        {
          "const": "capabilityUnavailable",
          "description": "The selected negotiated or provider capability is absent.",
          "type": "string"
        },
        {
          "const": "modelUnavailable",
          "description": "The provider no longer accepts the model selection.",
          "type": "string"
        },
        {
          "const": "nativeCatalogueUnsupported",
          "description": "The provider has no registered official native catalogue.",
          "type": "string"
        },
        {
          "const": "sessionNotFound",
          "description": "The Runtime session does not exist in the caller's grant.",
          "type": "string"
        },
        {
          "const": "terminalNotFound",
          "description": "The terminal does not exist in the connected Runtime generation.",
          "type": "string"
        },
        {
          "const": "terminalGenerationUnavailable",
          "description": "The descriptor's recorded Runtime generation is absent or cannot be queried.",
          "type": "string"
        },
        {
          "const": "terminalGone",
          "description": "The recorded Runtime generation answered but the terminal has ended.",
          "type": "string"
        },
        {
          "const": "terminalAlreadyLive",
          "description": "The provider-native conversation is already live as a terminal in another generation.",
          "type": "string"
        },
        {
          "const": "terminalWorkspaceConflict",
          "description": "The provider-native terminal is already live in another canonical workspace.",
          "type": "string"
        },
        {
          "const": "nativeConversationBusy",
          "description": "The provider-native conversation is already live on the structured session surface.",
          "type": "string"
        },
        {
          "const": "legacyGenerationBusy",
          "description": "A pre-public draining generation may still own an unexportable native conversation.",
          "type": "string"
        },
        {
          "const": "sessionConflict",
          "description": "The requested lifecycle transition conflicts with current state.",
          "type": "string"
        },
        {
          "const": "controlConflict",
          "description": "Another integration controls the session.",
          "type": "string"
        },
        {
          "const": "leaseExpired",
          "description": "The supplied control lease has expired.",
          "type": "string"
        },
        {
          "const": "workspaceConflict",
          "description": "The working tree already has an incompatible writer.",
          "type": "string"
        },
        {
          "const": "approvalExpired",
          "description": "The pending approval has expired.",
          "type": "string"
        },
        {
          "const": "approvalOptionInvalid",
          "description": "The chosen approval option is not currently offered.",
          "type": "string"
        },
        {
          "const": "idempotencyConflict",
          "description": "One mutation ID was reused with different parameters.",
          "type": "string"
        },
        {
          "const": "outcomeUnknown",
          "description": "A mutation may have happened and cannot be repeated safely.",
          "type": "string"
        },
        {
          "const": "resourceExhausted",
          "description": "A bounded resource has reached its advertised limit.",
          "type": "string"
        },
        {
          "const": "rateLimited",
          "description": "The caller exceeded a bounded admission rate.",
          "type": "string"
        },
        {
          "const": "gap",
          "description": "A reconnect cursor is outside bounded replay.",
          "type": "string"
        },
        {
          "const": "invalidRequest",
          "description": "The JSON-RPC request or its closed parameters are invalid.",
          "type": "string"
        },
        {
          "const": "methodNotFound",
          "description": "The public method name does not exist.",
          "type": "string"
        },
        {
          "const": "internal",
          "description": "Runtime failed without exposing private implementation detail.",
          "type": "string"
        }
      ]
    },
    "RuntimeEventNotification": {
      "additionalProperties": false,
      "description": "One provider-neutral normalized event notification.",
      "properties": {
        "event": {
          "description": "Existing normalized structural event, transported without interpretation by Runtime."
        },
        "eventRevision": {
          "$ref": "#/$defs/ProtocolRevision",
          "description": "Public event vocabulary revision."
        },
        "nextExpected": {
          "$ref": "#/$defs/EventCursor",
          "description": "Next exact reconnect boundary."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact watched session."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "sessionId",
        "eventRevision",
        "event",
        "nextExpected"
      ],
      "type": "object"
    },
    "RuntimeGeneration": {
      "additionalProperties": false,
      "description": "One running daemon build and where it listens.",
      "properties": {
        "controlEndpoint": {
          "description": "Exact local private control endpoint of this generation, for the executable's own command surface.",
          "type": "string"
        },
        "digest": {
          "description": "SHA-256 of the daemon executable, lowercase hex. The generation's identity.",
          "type": "string"
        },
        "draining": {
          "description": "Whether a newer generation has taken over new work from this one.",
          "type": "boolean"
        },
        "endpoint": {
          "description": "Exact local public Runtime endpoint of this generation.",
          "type": "string"
        },
        "endpointKind": {
          "$ref": "#/$defs/RuntimeEndpointKind",
          "description": "Platform-local transport kind of both endpoints."
        },
        "liveSessions": {
          "description": "Conversations this generation still has mid-turn. A draining generation exits when this reaches zero.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "processId": {
          "description": "Generation process identifier, used only for stale-state diagnostics.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "runtimeVersion": {
          "description": "Runtime product version of this generation.",
          "type": "string"
        },
        "startedAtMs": {
          "description": "When this generation published itself, Unix milliseconds. The newest is the one new work goes to.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "digest",
        "endpointKind",
        "endpoint",
        "controlEndpoint",
        "runtimeVersion",
        "processId",
        "startedAtMs",
        "liveSessions",
        "draining"
      ],
      "type": "object"
    },
    "RuntimeInstance": {
      "description": "Public Runtime instance facts used to reject a stale or replaced locator.",
      "properties": {
        "buildDigest": {
          "description": "SHA-256 of the running executable, measured at boot. Absent on runtimes older than\n2026-08-20; a manager that installed the binary treats absence or mismatch as an older\ndaemon and supersedes it instead of speaking a possibly different dialect past the hello.",
          "type": [
            "string",
            "null"
          ]
        },
        "instanceId": {
          "description": "Changes when a newly installed Runtime home is created.",
          "type": "string"
        },
        "platform": {
          "description": "Target class of the running artifact.",
          "type": "string"
        },
        "version": {
          "description": "Product version, not the compatibility decision.",
          "type": "string"
        }
      },
      "required": [
        "instanceId",
        "version",
        "platform"
      ],
      "type": "object"
    },
    "RuntimeLimits": {
      "description": "Numeric public bounds advertised during initialization.",
      "properties": {
        "challengeLifetimeMs": {
          "description": "Lifetime of one server-first challenge.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "controlLeaseLifetimeMs": {
          "description": "Lifetime of one renewable control lease.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enrollmentLifetimeMs": {
          "description": "Lifetime of one pending local integration enrollment.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "idempotencyWindowMs": {
          "description": "Maximum age of a mutation identity with a known outcome.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "maxAttachmentBase64Bytes": {
          "default": 0,
          "description": "Maximum base64 characters for one attached image. Zero on runtimes that predate block\nsubmission (before 2026-08-20): absent means the feature does not exist there.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxFrameBytes": {
          "description": "Maximum frame payload before allocation.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxIdempotencyRecords": {
          "description": "Maximum retained mutation identities across integrations.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxInputBlocks": {
          "default": 0,
          "description": "Maximum blocks in one submitted block set. Zero when block submission does not exist.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxInputBytes": {
          "description": "Maximum caller input bytes.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxInputImages": {
          "default": 0,
          "description": "Maximum images in one submitted block set. Zero when block submission does not exist.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxModelSelectionBytes": {
          "description": "Maximum opaque model selection bytes on session start.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxNativeAdoptionTokenBytes": {
          "description": "Maximum authenticated native adoption token bytes.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxNativePublicCursorBytes": {
          "description": "Maximum wrapped native catalogue cursor bytes.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxPageItems": {
          "description": "Maximum items in one catalogue page.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxPendingEnrollments": {
          "description": "Maximum active pending integration enrollments.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxReasoningSelectionBytes": {
          "description": "Maximum opaque reasoning effort selection bytes on session start.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxRevisionOffers": {
          "description": "Maximum finalized protocol revisions accepted in one offer.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxSubscriptions": {
          "description": "Maximum subscriptions on one connection.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalColumns": {
          "default": 0,
          "description": "Maximum shared PTY columns.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalIndexItems": {
          "default": 0,
          "description": "Maximum terminal descriptors returned by one generation.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalOutputBytes": {
          "default": 0,
          "description": "Maximum decoded exact bytes in one terminal output notification.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalRows": {
          "default": 0,
          "description": "Maximum shared PTY rows.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalScreenBytes": {
          "default": 0,
          "description": "Maximum decoded bytes in one terminal screen snapshot.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalViewQueueChunks": {
          "default": 0,
          "description": "Maximum queued output chunks per terminal view before an explicit lag boundary.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "maxTerminalWriteBytes": {
          "default": 0,
          "description": "Maximum decoded exact bytes accepted by one terminal write.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "nativeCursorLifetimeMs": {
          "description": "Lifetime of native catalogue cursors and adoption observations.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "maxFrameBytes",
        "maxInputBytes",
        "maxModelSelectionBytes",
        "maxReasoningSelectionBytes",
        "maxNativeAdoptionTokenBytes",
        "maxNativePublicCursorBytes",
        "maxPageItems",
        "maxSubscriptions",
        "challengeLifetimeMs",
        "enrollmentLifetimeMs",
        "maxPendingEnrollments",
        "maxRevisionOffers",
        "controlLeaseLifetimeMs",
        "idempotencyWindowMs",
        "maxIdempotencyRecords",
        "nativeCursorLifetimeMs"
      ],
      "type": "object"
    },
    "RuntimeLocatorRecord": {
      "additionalProperties": false,
      "description": "Operational bootstrap data published only after each generation's public endpoint is ready.",
      "properties": {
        "generations": {
          "description": "Every daemon generation currently serving this home, oldest start first.",
          "items": {
            "$ref": "#/$defs/RuntimeGeneration"
          },
          "type": "array"
        },
        "instanceId": {
          "description": "Durable installed Runtime identity, shared by every generation of one home.",
          "type": "string"
        },
        "schema": {
          "description": "Locator schema version.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema",
        "instanceId",
        "generations"
      ],
      "type": "object"
    },
    "RuntimeMethod": {
      "description": "A public Runtime method implemented by the initial read-only boundary.",
      "oneOf": [
        {
          "const": "runtime/initialize",
          "description": "Negotiate a public revision and bind the Runtime instance.",
          "type": "string"
        },
        {
          "const": "runtime/initialized",
          "description": "Finish initialization before any inventory request.",
          "type": "string"
        },
        {
          "const": "runtime/challenge",
          "description": "Server-first connection-bound authentication challenge notification.",
          "type": "string"
        },
        {
          "const": "integrations/requestEnrollment",
          "description": "Create a bounded pending local enrollment.",
          "type": "string"
        },
        {
          "const": "integrations/watchEnrollment",
          "description": "Read the current decision for the proved pending key.",
          "type": "string"
        },
        {
          "const": "integrations/getGrant",
          "description": "Read the authenticated integration's current grant.",
          "type": "string"
        },
        {
          "const": "integrations/rotateKey",
          "description": "Replace the authenticated integration key after exact local confirmation.",
          "type": "string"
        },
        {
          "const": "providers/usage",
          "description": "Read each account's latest reported position against its limits.",
          "type": "string"
        },
        {
          "const": "providers/list",
          "description": "Read the structural provider inventory.",
          "type": "string"
        },
        {
          "const": "providers/watch",
          "description": "Watch structural provider inventory changes without polling.",
          "type": "string"
        },
        {
          "const": "providers/getCapabilities",
          "description": "Discover structural lifecycle and event capabilities for one provider.",
          "type": "string"
        },
        {
          "const": "providers/listModels",
          "description": "Discover the selected provider's current opaque model catalogue.",
          "type": "string"
        },
        {
          "const": "providers/listNativeSessions",
          "description": "Discover one root-scoped official provider-native session page.",
          "type": "string"
        },
        {
          "const": "providers/nativeActivity",
          "description": "Name the conversations of one provider written in the last few seconds, which is how a caller knows a\nturn is running in a conversation this Runtime did not start.",
          "type": "string"
        },
        {
          "const": "sessions/list",
          "description": "Read the Runtime-managed session catalogue.",
          "type": "string"
        },
        {
          "const": "sessions/watchIndex",
          "description": "Watch authorized managed-session index snapshots without polling.",
          "type": "string"
        },
        {
          "const": "sessions/get",
          "description": "Read one exact Runtime-managed session descriptor.",
          "type": "string"
        },
        {
          "const": "sessions/start",
          "description": "Start one fresh provider-native session under an approved root.",
          "type": "string"
        },
        {
          "const": "sessions/adoptNative",
          "description": "Adopt one officially listed native session into Runtime supervision.",
          "type": "string"
        },
        {
          "const": "sessions/resume",
          "description": "Heat one existing Runtime-managed cold session.",
          "type": "string"
        },
        {
          "const": "sessions/acquireControl",
          "description": "Acquire the one renewable write lease for a live session.",
          "type": "string"
        },
        {
          "const": "sessions/renewControl",
          "description": "Renew one exact control lease generation.",
          "type": "string"
        },
        {
          "const": "sessions/releaseControl",
          "description": "Voluntarily release one exact control lease generation.",
          "type": "string"
        },
        {
          "const": "sessions/submitInput",
          "description": "Submit caller-owned input without rewriting or implicit retry.",
          "type": "string"
        },
        {
          "const": "sessions/submitBlocks",
          "description": "Submit caller-owned typed content blocks (text and images) without rewriting.",
          "type": "string"
        },
        {
          "const": "sessions/setModel",
          "description": "Switch the answering model under the current control lease.",
          "type": "string"
        },
        {
          "const": "sessions/setMode",
          "description": "Switch the governing permission mode under the current control lease.",
          "type": "string"
        },
        {
          "const": "sessions/watchEvents",
          "description": "Watch the existing bounded normalized event stream.",
          "type": "string"
        },
        {
          "const": "sessions/interrupt",
          "description": "Interrupt one exact controlled live session.",
          "type": "string"
        },
        {
          "const": "sessions/cool",
          "description": "Release one idle hot provider process while retaining its managed pointer.",
          "type": "string"
        },
        {
          "const": "sessions/forget",
          "description": "Forget one cold Runtime pointer after local confirmation.",
          "type": "string"
        },
        {
          "const": "sessions/deleteNative",
          "description": "Delete one provider-native conversation through the provider's own surface.",
          "type": "string"
        },
        {
          "const": "sessions/archiveNative",
          "description": "Archive one provider-native conversation through the provider's own surface.",
          "type": "string"
        },
        {
          "const": "terminals/list",
          "description": "Return the caller-visible live terminals in this Runtime generation.",
          "type": "string"
        },
        {
          "const": "terminals/watchIndex",
          "description": "Watch root-filtered terminal index snapshots.",
          "type": "string"
        },
        {
          "const": "terminals/open",
          "description": "Open a fresh terminal or resume one authorized native conversation.",
          "type": "string"
        },
        {
          "const": "terminals/attach",
          "description": "Attach one connection-bound view at current shared geometry.",
          "type": "string"
        },
        {
          "const": "terminals/acquireControl",
          "description": "Acquire the one renewable terminal write lease.",
          "type": "string"
        },
        {
          "const": "terminals/renewControl",
          "description": "Renew one exact terminal lease generation.",
          "type": "string"
        },
        {
          "const": "terminals/releaseControl",
          "description": "Voluntarily release one exact terminal lease generation.",
          "type": "string"
        },
        {
          "const": "terminals/write",
          "description": "Write exact caller-owned bytes once under the current terminal lease.",
          "type": "string"
        },
        {
          "const": "terminals/resize",
          "description": "Set bounded shared PTY geometry under the current terminal lease.",
          "type": "string"
        },
        {
          "const": "terminals/detach",
          "description": "Detach only one connection-bound terminal view.",
          "type": "string"
        },
        {
          "const": "terminals/stop",
          "description": "Stop one hosted provider CLI process under the current terminal lease.",
          "type": "string"
        },
        {
          "const": "approvals/listPending",
          "description": "Read pending structured provider approvals for one controlled session.",
          "type": "string"
        },
        {
          "const": "approvals/respond",
          "description": "Answer one exact pending provider approval.",
          "type": "string"
        },
        {
          "const": "sessions/indexChanged",
          "description": "One changed managed-session index snapshot.",
          "type": "string"
        },
        {
          "const": "sessions/indexEnded",
          "description": "Final managed-session index subscription reason.",
          "type": "string"
        },
        {
          "const": "providers/changed",
          "description": "One changed provider inventory snapshot.",
          "type": "string"
        },
        {
          "const": "providers/watchEnded",
          "description": "Final provider inventory subscription reason.",
          "type": "string"
        },
        {
          "const": "providers/usageChanged",
          "description": "One changed account usage snapshot, on the same provider subscription.",
          "type": "string"
        },
        {
          "const": "sessions/event",
          "description": "One normalized event notification.",
          "type": "string"
        },
        {
          "const": "sessions/lagged",
          "description": "A bounded subscription was retired after lagging.",
          "type": "string"
        },
        {
          "const": "terminals/indexChanged",
          "description": "One changed root-filtered terminal index snapshot.",
          "type": "string"
        },
        {
          "const": "terminals/indexEnded",
          "description": "Final terminal index subscription reason.",
          "type": "string"
        },
        {
          "const": "terminals/output",
          "description": "One bounded exact output chunk for a terminal view.",
          "type": "string"
        },
        {
          "const": "terminals/lagged",
          "description": "Explicit lost-byte boundary with a replacement screen snapshot.",
          "type": "string"
        },
        {
          "const": "terminals/exited",
          "description": "Provider process exit after preceding output drained.",
          "type": "string"
        },
        {
          "const": "runtime/panicStop",
          "description": "Stop every supervised process in the safe direction.",
          "type": "string"
        }
      ]
    },
    "RuntimeModelCatalog": {
      "description": "The current model information Runtime can truthfully expose.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "An official provider surface returned a bounded authoritative list.",
          "properties": {
            "coverage": {
              "const": "known",
              "type": "string"
            },
            "models": {
              "description": "Choices in provider order.",
              "items": {
                "$ref": "#/$defs/RuntimeModelChoice"
              },
              "type": "array"
            }
          },
          "required": [
            "coverage",
            "models"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The provider exposes selectable aliases but no complete inventory.",
          "properties": {
            "aliases": {
              "description": "Exact opaque tokens accepted by the provider.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "coverage": {
              "const": "aliases",
              "type": "string"
            },
            "reasoningEfforts": {
              "description": "Provider-owned reasoning choices that apply across those aliases.",
              "items": {
                "$ref": "#/$defs/RuntimeReasoningChoice"
              },
              "type": "array"
            },
            "why": {
              "description": "Safe structural explanation of the limitation.",
              "type": "string"
            }
          },
          "required": [
            "coverage",
            "aliases",
            "reasoningEfforts",
            "why"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "An official or provider-owned surface returned a structurally limited list.",
          "properties": {
            "aliases": {
              "description": "Exact opaque alias tokens accepted by the provider.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "coverage": {
              "const": "partial",
              "type": "string"
            },
            "models": {
              "description": "Exact model options observed from the provider.",
              "items": {
                "$ref": "#/$defs/RuntimeModelChoice"
              },
              "type": "array"
            },
            "reasoningEfforts": {
              "description": "Provider-owned reasoning choices used when a model has no narrower list.",
              "items": {
                "$ref": "#/$defs/RuntimeReasoningChoice"
              },
              "type": "array"
            },
            "why": {
              "description": "Safe structural explanation of the limitation.",
              "type": "string"
            }
          },
          "required": [
            "coverage",
            "aliases",
            "models",
            "reasoningEfforts",
            "why"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Runtime probed but cannot currently interpret a catalogue structurally.",
          "properties": {
            "coverage": {
              "const": "unknown",
              "type": "string"
            },
            "why": {
              "description": "Safe structural explanation of the unknown result.",
              "type": "string"
            }
          },
          "required": [
            "coverage",
            "why"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The registered driver has no official model discovery capability.",
          "properties": {
            "coverage": {
              "const": "unsupported",
              "type": "string"
            },
            "why": {
              "description": "Safe structural explanation of the missing capability.",
              "type": "string"
            }
          },
          "required": [
            "coverage",
            "why"
          ],
          "type": "object"
        }
      ]
    },
    "RuntimeModelChoice": {
      "additionalProperties": false,
      "description": "One opaque model selection reported by a provider.",
      "properties": {
        "description": {
          "description": "Provider-supplied structural description.",
          "type": "string"
        },
        "displayName": {
          "description": "Provider-supplied presentation name.",
          "type": "string"
        },
        "id": {
          "description": "Exact value to return when starting a session.",
          "type": "string"
        },
        "isDefault": {
          "description": "Whether the provider currently marks this choice as its default.",
          "type": "boolean"
        },
        "reasoningEfforts": {
          "description": "Opaque reasoning choices reported for this model.",
          "items": {
            "$ref": "#/$defs/RuntimeReasoningChoice"
          },
          "type": "array"
        }
      },
      "required": [
        "id",
        "displayName",
        "description",
        "isDefault",
        "reasoningEfforts"
      ],
      "type": "object"
    },
    "RuntimeProviderCapabilities": {
      "additionalProperties": false,
      "description": "Structural lifecycle and event capabilities for one exact provider installation.",
      "properties": {
        "approvals": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "Structured provider-native approvals can be answered."
        },
        "cooling": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "A hot process can be released without deleting provider-native state."
        },
        "freshSession": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "A fresh session can be opened."
        },
        "freshness": {
          "$ref": "#/$defs/CapabilityFreshness",
          "description": "Whether these observations match the current binary identity."
        },
        "interrupt": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "A running turn can receive an interrupt request."
        },
        "nativeSessionArchive": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderCapabilityObservation"
            },
            {
              "type": "null"
            }
          ],
          "description": "A stored provider-native conversation can be archived through the provider's own surface.\n\nOptional for lockstep-additive compatibility with older Runtime versions."
        },
        "nativeSessionCatalogue": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "An official provider-native session catalogue may be requested."
        },
        "nativeSessionDelete": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderCapabilityObservation"
            },
            {
              "type": "null"
            }
          ],
          "description": "A stored provider-native conversation can be deleted through the provider's own surface.\n\nOptional for the same lockstep-additive reason as the two above: absent when an older Runtime\nanswers, never guessed at. A surface offers the act only where this says it exists."
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity selected by the caller."
        },
        "resume": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "A provider-native session can be resumed."
        },
        "setModel": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderCapabilityObservation"
            },
            {
              "type": "null"
            }
          ],
          "description": "The answering model can be switched mid-session.\n\nOptional for the same lockstep-additive reason as `waitingOn`: absent when an older Runtime\nanswers, never guessed at."
        },
        "setReasoningEffort": {
          "anyOf": [
            {
              "$ref": "#/$defs/ProviderCapabilityObservation"
            },
            {
              "type": "null"
            }
          ],
          "description": "The reasoning effort can be switched mid-session.\n\nDistinct from the catalogue's per-model efforts: a CLI can offer an effort at open time and\nrefuse to change it afterwards, and this is what lets a surface say \"applies from the next\nsession\" before the attempt instead of after the refusal."
        },
        "structuredEvents": {
          "$ref": "#/$defs/ProviderCapabilityObservation",
          "description": "Provider output is mapped to structured Runtime events."
        }
      },
      "required": [
        "providerId",
        "freshness",
        "freshSession",
        "resume",
        "structuredEvents",
        "interrupt",
        "approvals",
        "cooling",
        "nativeSessionCatalogue"
      ],
      "type": "object"
    },
    "RuntimeReasoningChoice": {
      "additionalProperties": false,
      "description": "One opaque reasoning-effort option reported by a provider.",
      "properties": {
        "description": {
          "description": "Provider-supplied structural description.",
          "type": "string"
        },
        "id": {
          "description": "Exact value accepted by the provider.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "description"
      ],
      "type": "object"
    },
    "RuntimeSessionId": {
      "description": "A stable Runtime-managed session identity.",
      "type": "string"
    },
    "RuntimeTerminalId": {
      "description": "One hosted provider TUI process in one Runtime generation.",
      "type": "string"
    },
    "RuntimeTerminalViewId": {
      "description": "One connection-bound terminal output subscription.",
      "type": "string"
    },
    "ServerChallenge": {
      "additionalProperties": false,
      "description": "One connection-bound challenge sent before initialization.",
      "properties": {
        "expiresAtMs": {
          "description": "Wall-clock expiry in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "instanceId": {
          "description": "Runtime instance named by the verified locator.",
          "type": "string"
        },
        "nonce": {
          "description": "Base64url random challenge bytes.",
          "type": "string"
        },
        "nonceId": {
          "description": "Opaque one-use nonce identity.",
          "type": "string"
        }
      },
      "required": [
        "instanceId",
        "nonceId",
        "nonce",
        "expiresAtMs"
      ],
      "type": "object"
    },
    "SessionDescriptor": {
      "additionalProperties": false,
      "description": "One Runtime-managed session in the immediate catalogue.",
      "properties": {
        "hot": {
          "description": "Whether Runtime currently owns a provider process for this session.",
          "type": "boolean"
        },
        "label": {
          "description": "Operator-owned label, never derived from conversation content.",
          "type": [
            "string",
            "null"
          ]
        },
        "lifecycle": {
          "$ref": "#/$defs/LifecycleState",
          "description": "Current supervision state."
        },
        "looksStuck": {
          "description": "Structural stalled-turn hint derived by Core without interpreting conversation content.",
          "type": "boolean"
        },
        "memoryBytes": {
          "description": "Resident memory of the provider process behind this session, in bytes, as the operating system reports\nit at listing time.\n\nPresent only while Runtime owns a process for the session and the operating system answered. A driver\nthat shares one process among sessions reports that process for each of them. Never estimated and never\nzero-filled: absent means not measured.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "nativeSessionId": {
          "description": "Provider-owned resume identity when the official live or stored pointer exposes one.",
          "type": [
            "string",
            "null"
          ]
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider selection value."
        },
        "sessionGeneration": {
          "description": "Monotonic lifecycle generation used to reject stale control actions.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Stable Runtime selection value."
        },
        "waitingOn": {
          "anyOf": [
            {
              "$ref": "#/$defs/WaitingOn"
            },
            {
              "type": "null"
            }
          ],
          "description": "What the running turn is waiting for, when Core observed it stop for something.\n\nOptional and omitted when absent, so a client written against this revision before the field existed\nreads exactly what it read before. Derived from the provider's own structural turn frames, never from\nconversation content."
        },
        "workspace": {
          "description": "Exact canonical workspace display path, already filtered through the integration's approved roots.",
          "type": "string"
        }
      },
      "required": [
        "sessionId",
        "providerId",
        "workspace",
        "hot",
        "lifecycle",
        "looksStuck",
        "sessionGeneration"
      ],
      "type": "object"
    },
    "SessionIndexChangedNotification": {
      "additionalProperties": false,
      "description": "A changed authorized managed-session snapshot.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/ManagedSessionList",
          "description": "New complete authorized snapshot."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "SessionIndexEndReason": {
      "description": "Why a managed-session index subscription ended.",
      "oneOf": [
        {
          "const": "integrationRevoked",
          "description": "The integration grant was revoked.",
          "type": "string"
        },
        {
          "const": "authorityChanged",
          "description": "Scope, roots, or integration generations changed and require authenticated reconnect.",
          "type": "string"
        },
        {
          "const": "rootDenied",
          "description": "A previously approved filesystem root no longer names the approved object.",
          "type": "string"
        },
        {
          "const": "runtimeUnavailable",
          "description": "The Runtime catalogue became unavailable.",
          "type": "string"
        }
      ]
    },
    "SessionIndexEndedNotification": {
      "additionalProperties": false,
      "description": "Final typed reason for retiring a managed-session index subscription.",
      "properties": {
        "reason": {
          "$ref": "#/$defs/SessionIndexEndReason",
          "description": "Structural terminal reason."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "reason"
      ],
      "type": "object"
    },
    "SessionOpenResult": {
      "additionalProperties": false,
      "description": "One newly supervised or reheated session and its initial controller authority.",
      "properties": {
        "control": {
          "$ref": "#/$defs/ControlLease",
          "description": "Initial short-lived control lease held by the integration that opened it."
        },
        "session": {
          "$ref": "#/$defs/SessionDescriptor",
          "description": "Current structural managed-session descriptor."
        }
      },
      "required": [
        "session",
        "control"
      ],
      "type": "object"
    },
    "SessionWorkspaceAccess": {
      "description": "Whether a newly heated process must be the only writer for its working tree.",
      "oneOf": [
        {
          "const": "exclusive",
          "description": "Refuse any overlapping opening, live, or closing writer.",
          "type": "string"
        },
        {
          "const": "shared",
          "description": "The local operator explicitly approved concurrent writers for this operation.",
          "type": "string"
        }
      ]
    },
    "SetModeParams": {
      "additionalProperties": false,
      "description": "Switch the governing permission mode under one exact control lease.\n\nA relay like the model switch: the mode is the operator's choice by the provider's own name for it, and\nwhether it changed stays the provider's word, arriving on the event stream as its own event. Which names\nmay travel at all is bounded before relay (manifest-listed for CLIs whose vocabulary is not discoverable,\nsession-announced for protocols that announce one), so a mode that removes safety prompts entirely is not\na reachable destination through this method.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "mode": {
          "description": "The mode, by the provider's own name for it, exactly as the operator chose it.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration",
        "mode"
      ],
      "type": "object"
    },
    "SetModelParams": {
      "additionalProperties": false,
      "description": "Switch the answering model under one exact control lease.\n\nA relay of the operator's choice through the provider's own switch surface, never a rewrite: what the\nsession actually answers with stays the provider's word, arriving on the event stream as its own event.\nA provider whose surface cannot carry the request refuses loudly instead of dropping any part of it.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "model": {
          "description": "The model, by the provider's own name for it, exactly as the operator chose it.",
          "type": "string"
        },
        "reasoningEffort": {
          "description": "The provider-owned reasoning posture, when the operator chose one and the provider can carry it.",
          "type": [
            "string",
            "null"
          ]
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration",
        "model"
      ],
      "type": "object"
    },
    "StartSessionParams": {
      "additionalProperties": false,
      "description": "Start a new provider-native session in one exact authorized workspace.",
      "properties": {
        "access": {
          "$ref": "#/$defs/SessionWorkspaceAccess",
          "description": "Writer collision posture for the working tree."
        },
        "model": {
          "description": "Exact opaque model choice previously returned by Runtime, or provider default when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "permission": {
          "description": "Exact permission mode to start at, or provider default when absent.\n\nThe value must sit inside the provider's declared switchable vocabulary, the same boundary\n`sessions/setMode` enforces, so starting a session can never reach a mode that switching one\ncould not. Modes that remove safety prompts are outside that vocabulary for every caller.",
          "type": [
            "string",
            "null"
          ]
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by Runtime inventory."
        },
        "reasoningEffort": {
          "description": "Exact opaque reasoning choice returned for the selected model, or provider default when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "workspace": {
          "description": "Exact workspace path under a current approved root.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "providerId",
        "workspace",
        "access"
      ],
      "type": "object"
    },
    "SubmitBlocksParams": {
      "additionalProperties": false,
      "description": "Submit typed caller-owned content blocks under one exact control lease.",
      "properties": {
        "blocks": {
          "description": "The blocks, in the order the caller composed them.",
          "items": {
            "$ref": "#/$defs/PublicInputBlock"
          },
          "type": "array"
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration",
        "blocks"
      ],
      "type": "object"
    },
    "SubmitInputParams": {
      "additionalProperties": false,
      "description": "Submit caller-owned input under one exact control lease.",
      "properties": {
        "input": {
          "description": "Caller-owned input transported without prefix, suffix, or rewriting.",
          "type": "string"
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "requestId",
        "sessionId",
        "leaseId",
        "leaseGeneration",
        "input"
      ],
      "type": "object"
    },
    "SuccessResponse": {
      "additionalProperties": false,
      "description": "Successful JSON-RPC response.",
      "properties": {
        "id": {
          "$ref": "#/$defs/JsonRpcId",
          "description": "Request identifier copied unchanged."
        },
        "jsonrpc": {
          "description": "Must be exactly `2.0`.",
          "type": "string"
        },
        "result": {
          "description": "Typed method result represented at the envelope boundary."
        }
      },
      "required": [
        "jsonrpc",
        "id",
        "result"
      ],
      "type": "object"
    },
    "TerminalAcquireControlParams": {
      "additionalProperties": false,
      "description": "Acquire control only from one exact observed live incarnation.",
      "properties": {
        "expectedTerminalGeneration": {
          "description": "Process incarnation visible when the user chose the action.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact terminal in the connected Runtime generation."
        }
      },
      "required": [
        "requestId",
        "terminalId",
        "expectedTerminalGeneration"
      ],
      "type": "object"
    },
    "TerminalAttachParams": {
      "additionalProperties": false,
      "description": "Attach one new view at the terminal's current shared geometry.",
      "properties": {
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact terminal in the connected Runtime generation."
        }
      },
      "required": [
        "terminalId"
      ],
      "type": "object"
    },
    "TerminalControlLease": {
      "additionalProperties": false,
      "description": "One renewable write authority for one exact terminal process incarnation.",
      "properties": {
        "expiresAtMs": {
          "description": "Wall-clock expiry in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseGeneration": {
          "description": "Monotonic lease generation required on renewal and mutations.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque unguessable lease identity.",
          "type": "string"
        },
        "terminalGeneration": {
          "description": "Process incarnation observed at acquisition.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact controlled terminal."
        }
      },
      "required": [
        "leaseId",
        "terminalId",
        "terminalGeneration",
        "leaseGeneration",
        "expiresAtMs"
      ],
      "type": "object"
    },
    "TerminalControlParams": {
      "additionalProperties": false,
      "description": "Renew or release one exact terminal control lease generation.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact controlled terminal."
        }
      },
      "required": [
        "requestId",
        "terminalId",
        "leaseId",
        "leaseGeneration"
      ],
      "type": "object"
    },
    "TerminalDescriptor": {
      "additionalProperties": false,
      "description": "One live terminal descriptor visible through an approved root.",
      "properties": {
        "geometry": {
          "$ref": "#/$defs/TerminalGeometry",
          "description": "Current shared PTY geometry."
        },
        "memoryBytes": {
          "description": "Resident memory of the hosted process in bytes, as the operating system reports it at listing time.\n\nPresent only while the process runs and the operating system answered; absent means not measured.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "nativeSessionId": {
          "description": "Provider-owned durable identity only when it was known before the terminal opened.",
          "type": [
            "string",
            "null"
          ]
        },
        "openedAtMs": {
          "description": "Runtime wall time when the process was opened, in Unix milliseconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "processState": {
          "$ref": "#/$defs/TerminalProcessState",
          "description": "Structural live process state."
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by Runtime inventory."
        },
        "runtimeGeneration": {
          "description": "SHA-256 digest of the Runtime generation that owns the process.",
          "type": "string"
        },
        "terminalGeneration": {
          "description": "Monotonic process incarnation used to reject stale control acquisition.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Runtime-local terminal identity."
        },
        "workspace": {
          "description": "Exact canonical workspace already filtered through the caller's grant.",
          "type": "string"
        }
      },
      "required": [
        "terminalId",
        "runtimeGeneration",
        "providerId",
        "workspace",
        "processState",
        "openedAtMs",
        "terminalGeneration",
        "geometry"
      ],
      "type": "object"
    },
    "TerminalDetachParams": {
      "additionalProperties": false,
      "description": "Detach only one connection-bound view without stopping the provider process.",
      "properties": {
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact terminal."
        },
        "viewId": {
          "$ref": "#/$defs/RuntimeTerminalViewId",
          "description": "Exact view created on this connection."
        }
      },
      "required": [
        "terminalId",
        "viewId"
      ],
      "type": "object"
    },
    "TerminalExitedNotification": {
      "additionalProperties": false,
      "description": "Provider process exit delivered after preceding output has drained.",
      "properties": {
        "exitCode": {
          "description": "Provider process exit code as reported by the terminal host.",
          "format": "int32",
          "type": "integer"
        },
        "viewId": {
          "$ref": "#/$defs/RuntimeTerminalViewId",
          "description": "Exact connection-bound view."
        }
      },
      "required": [
        "viewId",
        "exitCode"
      ],
      "type": "object"
    },
    "TerminalGeometry": {
      "additionalProperties": false,
      "description": "Shared PTY geometry visible to every attached viewer.",
      "properties": {
        "columns": {
          "description": "PTY columns.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "rows": {
          "description": "PTY rows.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "columns",
        "rows"
      ],
      "type": "object"
    },
    "TerminalIndexChangedNotification": {
      "additionalProperties": false,
      "description": "Replace one connection's complete visible terminal index snapshot.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/TerminalIndexSnapshot",
          "description": "Complete new root-filtered snapshot."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "TerminalIndexEndReason": {
      "description": "Why a terminal index subscription ended.",
      "oneOf": [
        {
          "const": "integrationRevoked",
          "description": "The integration grant was revoked.",
          "type": "string"
        },
        {
          "const": "authorityChanged",
          "description": "Scope, root, key, or grant generation changed and requires reconnect.",
          "type": "string"
        },
        {
          "const": "runtimeUnavailable",
          "description": "The Runtime generation stopped publishing the index.",
          "type": "string"
        }
      ]
    },
    "TerminalIndexEndedNotification": {
      "additionalProperties": false,
      "description": "Final typed reason for retiring a terminal index subscription.",
      "properties": {
        "reason": {
          "$ref": "#/$defs/TerminalIndexEndReason",
          "description": "Structural end reason."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "reason"
      ],
      "type": "object"
    },
    "TerminalIndexSnapshot": {
      "additionalProperties": false,
      "description": "One root-filtered snapshot from one Runtime generation.",
      "properties": {
        "terminals": {
          "description": "Live descriptors in stable terminal identity order.",
          "items": {
            "$ref": "#/$defs/TerminalDescriptor"
          },
          "type": "array"
        },
        "warnings": {
          "description": "Safe structural omissions without unauthorized paths or terminal content.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "terminals",
        "warnings"
      ],
      "type": "object"
    },
    "TerminalLaggedNotification": {
      "additionalProperties": false,
      "description": "Explicit loss boundary followed atomically by one replacement screen snapshot.",
      "properties": {
        "lostChunks": {
          "description": "Number of broadcast chunks skipped before the replacement snapshot.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "nextSequence": {
          "description": "Sequence assigned to the next live output chunk.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "screenBase64": {
          "description": "Base64 of the bounded replacement screen.",
          "type": "string"
        },
        "viewId": {
          "$ref": "#/$defs/RuntimeTerminalViewId",
          "description": "Exact connection-bound view."
        }
      },
      "required": [
        "viewId",
        "lostChunks",
        "screenBase64",
        "nextSequence"
      ],
      "type": "object"
    },
    "TerminalOpenParams": {
      "additionalProperties": false,
      "description": "Open a fresh terminal or resume one authorized native conversation.",
      "properties": {
        "geometry": {
          "$ref": "#/$defs/TerminalGeometry",
          "description": "Initial bounded PTY geometry."
        },
        "providerId": {
          "$ref": "#/$defs/ProviderId",
          "description": "Opaque provider identity returned by Runtime inventory."
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity retained across outcome queries, never automatic retries."
        },
        "target": {
          "$ref": "#/$defs/TerminalOpenTarget",
          "description": "Fresh or exact authorized native resume intent."
        },
        "workspace": {
          "description": "Exact workspace path under a current approved root.",
          "type": "string"
        }
      },
      "required": [
        "requestId",
        "providerId",
        "workspace",
        "target",
        "geometry"
      ],
      "type": "object"
    },
    "TerminalOpenTarget": {
      "description": "Which provider-owned terminal conversation Runtime should host.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Start a fresh provider TUI without inventing a durable native identity.",
          "properties": {
            "kind": {
              "const": "fresh",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Resume one exact provider-native conversation from Runtime's authorized catalogue.",
          "properties": {
            "adoptionToken": {
              "description": "Short-lived Runtime proof for this exact authorized catalogue observation.",
              "type": "string"
            },
            "kind": {
              "const": "native",
              "type": "string"
            },
            "nativeSessionId": {
              "description": "Provider-owned opaque conversation identity.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "nativeSessionId",
            "adoptionToken"
          ],
          "type": "object"
        }
      ]
    },
    "TerminalOutputNotification": {
      "additionalProperties": false,
      "description": "One bounded exact output chunk for a terminal view.",
      "properties": {
        "bytesBase64": {
          "description": "Base64 of exact provider CLI output bytes.",
          "type": "string"
        },
        "sequence": {
          "description": "Monotonic sequence within this view, starting at one after its snapshot.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "viewId": {
          "$ref": "#/$defs/RuntimeTerminalViewId",
          "description": "Exact connection-bound view."
        }
      },
      "required": [
        "viewId",
        "sequence",
        "bytesBase64"
      ],
      "type": "object"
    },
    "TerminalProcessState": {
      "description": "Structural process state without terminal content.",
      "oneOf": [
        {
          "const": "running",
          "description": "The provider CLI process is live.",
          "type": "string"
        },
        {
          "const": "stopping",
          "description": "Runtime accepted a stop request and is waiting for process exit.",
          "type": "string"
        }
      ]
    },
    "TerminalResizeParams": {
      "additionalProperties": false,
      "description": "Set shared PTY geometry under one current terminal control lease.",
      "properties": {
        "geometry": {
          "$ref": "#/$defs/TerminalGeometry",
          "description": "New bounded shared geometry."
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted mutation identity."
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact controlled terminal."
        }
      },
      "required": [
        "requestId",
        "terminalId",
        "leaseId",
        "leaseGeneration",
        "geometry"
      ],
      "type": "object"
    },
    "TerminalStopParams": {
      "additionalProperties": false,
      "description": "Stop one hosted provider CLI under the exact current lease.",
      "properties": {
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity."
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact controlled terminal."
        }
      },
      "required": [
        "requestId",
        "terminalId",
        "leaseId",
        "leaseGeneration"
      ],
      "type": "object"
    },
    "TerminalViewOpened": {
      "additionalProperties": false,
      "description": "A view starts with one bounded screen snapshot, then receives live output notifications.",
      "properties": {
        "controlLease": {
          "anyOf": [
            {
              "$ref": "#/$defs/TerminalControlLease"
            },
            {
              "type": "null"
            }
          ],
          "description": "Initial write authority only when `open` was authorized for input."
        },
        "screenBase64": {
          "description": "Base64 of the current bounded terminal screen, delivered before live bytes.",
          "type": "string"
        },
        "terminal": {
          "$ref": "#/$defs/TerminalDescriptor",
          "description": "Current root-authorized terminal descriptor."
        },
        "viewId": {
          "$ref": "#/$defs/RuntimeTerminalViewId",
          "description": "Connection-bound output view."
        }
      },
      "required": [
        "terminal",
        "viewId",
        "screenBase64"
      ],
      "type": "object"
    },
    "TerminalWriteParams": {
      "additionalProperties": false,
      "description": "Send exact caller-owned bytes once under one current terminal control lease.",
      "properties": {
        "bytesBase64": {
          "description": "Base64 of exact caller-owned bytes.",
          "type": "string"
        },
        "leaseGeneration": {
          "description": "Exact current lease generation.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "leaseId": {
          "description": "Opaque lease identity returned on acquisition.",
          "type": "string"
        },
        "requestId": {
          "$ref": "#/$defs/MutationRequestId",
          "description": "Caller-minted idempotency identity. SDKs never resubmit terminal bytes automatically."
        },
        "terminalId": {
          "$ref": "#/$defs/RuntimeTerminalId",
          "description": "Exact controlled terminal."
        }
      },
      "required": [
        "requestId",
        "terminalId",
        "leaseId",
        "leaseGeneration",
        "bytesBase64"
      ],
      "type": "object"
    },
    "WaitingOn": {
      "description": "What a running turn is waiting for, when it is waiting for anybody.\n\nStructural, and deliberately only two values. A surface listing eight running sessions needs to answer one\nquestion without opening any of them: which of these stopped for me? An approval identifier or the provider's\nown wording would be conversation detail, which this protocol does not carry.",
      "oneOf": [
        {
          "const": "person",
          "description": "A person has to answer before the turn continues.",
          "type": "string"
        },
        {
          "const": "quota",
          "description": "An account limit has to lapse before the turn continues.",
          "type": "string"
        }
      ]
    },
    "WatchEnrollmentParams": {
      "additionalProperties": false,
      "description": "Read one pending decision on the same proved connection.",
      "properties": {
        "pendingId": {
          "$ref": "#/$defs/PendingEnrollmentId",
          "description": "Pending decision returned for this key."
        }
      },
      "required": [
        "pendingId"
      ],
      "type": "object"
    },
    "WatchEventsParams": {
      "additionalProperties": false,
      "description": "Install one bounded event subscription on a dedicated connection.",
      "properties": {
        "after": {
          "anyOf": [
            {
              "$ref": "#/$defs/EventCursor"
            },
            {
              "type": "null"
            }
          ],
          "description": "Next expected event, or no cursor for the current bounded tail."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact Runtime-managed session."
        }
      },
      "required": [
        "sessionId"
      ],
      "type": "object"
    },
    "WatchEventsResult": {
      "additionalProperties": false,
      "description": "Event subscription boundary returned before replay or live delivery.",
      "properties": {
        "gap": {
          "anyOf": [
            {
              "$ref": "#/$defs/EventGap"
            },
            {
              "type": "null"
            }
          ],
          "description": "Explicit bounded replay miss."
        },
        "liveAt": {
          "$ref": "#/$defs/EventCursor",
          "description": "Boundary between replay and newly published events."
        },
        "sessionId": {
          "$ref": "#/$defs/RuntimeSessionId",
          "description": "Exact watched session."
        },
        "startsAt": {
          "$ref": "#/$defs/EventCursor",
          "description": "First delivered boundary."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "sessionId",
        "startsAt",
        "liveAt"
      ],
      "type": "object"
    },
    "WatchProvidersParams": {
      "additionalProperties": false,
      "description": "Install one dedicated provider inventory subscription.",
      "type": "object"
    },
    "WatchProvidersResult": {
      "additionalProperties": false,
      "description": "Initial provider snapshot and connection-local subscription identity.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/ProviderList",
          "description": "Exact provider snapshot at the subscription boundary."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "WatchSessionIndexParams": {
      "additionalProperties": false,
      "description": "Install one dedicated managed-session index subscription.",
      "type": "object"
    },
    "WatchSessionIndexResult": {
      "additionalProperties": false,
      "description": "Initial authorized snapshot and connection-local subscription identity.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/ManagedSessionList",
          "description": "Exact authorized snapshot at the subscription boundary."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    },
    "WatchTerminalIndexParams": {
      "additionalProperties": false,
      "description": "Install one bounded terminal index subscription.",
      "type": "object"
    },
    "WatchTerminalIndexResult": {
      "additionalProperties": false,
      "description": "Initial terminal index and the connection-local subscription identity.",
      "properties": {
        "snapshot": {
          "$ref": "#/$defs/TerminalIndexSnapshot",
          "description": "Initial root-filtered snapshot."
        },
        "subscriptionId": {
          "description": "Opaque connection-local subscription identity.",
          "type": "string"
        }
      },
      "required": [
        "subscriptionId",
        "snapshot"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Root containing every type admitted by the initial public boundary.",
  "properties": {
    "acquire_control": {
      "$ref": "#/$defs/AcquireControlParams"
    },
    "adopt_native_session": {
      "$ref": "#/$defs/AdoptNativeSessionParams"
    },
    "archive_native_session": {
      "$ref": "#/$defs/ArchiveNativeSessionParams"
    },
    "challenge": {
      "$ref": "#/$defs/ServerChallenge"
    },
    "control_lease": {
      "$ref": "#/$defs/ControlLease"
    },
    "control_lease_params": {
      "$ref": "#/$defs/ControlLeaseParams"
    },
    "cool_session": {
      "$ref": "#/$defs/CoolSessionParams"
    },
    "delete_native_session": {
      "$ref": "#/$defs/DeleteNativeSessionParams"
    },
    "enrollment_decision": {
      "$ref": "#/$defs/EnrollmentDecision"
    },
    "enrollment_receipt": {
      "$ref": "#/$defs/EnrollmentReceipt"
    },
    "forget_session": {
      "$ref": "#/$defs/ForgetSessionParams"
    },
    "get_provider_capabilities": {
      "$ref": "#/$defs/GetProviderCapabilitiesParams"
    },
    "get_session": {
      "$ref": "#/$defs/GetSessionParams"
    },
    "initialize_params": {
      "$ref": "#/$defs/InitializeParams"
    },
    "initialize_result": {
      "$ref": "#/$defs/InitializeResult"
    },
    "integration_grant": {
      "$ref": "#/$defs/IntegrationGrant"
    },
    "lagged": {
      "$ref": "#/$defs/LaggedNotification"
    },
    "list_models": {
      "$ref": "#/$defs/ListModelsParams"
    },
    "list_native_sessions": {
      "$ref": "#/$defs/ListNativeSessionsParams"
    },
    "list_pending_approvals": {
      "$ref": "#/$defs/ListPendingApprovalsParams"
    },
    "list_terminals": {
      "$ref": "#/$defs/ListTerminalsParams"
    },
    "managed_session_list": {
      "$ref": "#/$defs/ManagedSessionList"
    },
    "method": {
      "$ref": "#/$defs/RuntimeMethod"
    },
    "model_catalogue": {
      "$ref": "#/$defs/RuntimeModelCatalog"
    },
    "native_activity": {
      "$ref": "#/$defs/NativeActivity"
    },
    "native_activity_params": {
      "$ref": "#/$defs/NativeActivityParams"
    },
    "native_session_catalogue": {
      "$ref": "#/$defs/NativeSessionCatalogue"
    },
    "notification": {
      "$ref": "#/$defs/JsonRpcNotification"
    },
    "pending_approvals": {
      "$ref": "#/$defs/PendingApprovalList"
    },
    "provider_capabilities": {
      "$ref": "#/$defs/RuntimeProviderCapabilities"
    },
    "provider_list": {
      "$ref": "#/$defs/ProviderList"
    },
    "provider_usage_list": {
      "$ref": "#/$defs/ProviderUsageList"
    },
    "provider_watch_ended": {
      "$ref": "#/$defs/ProviderWatchEndedNotification"
    },
    "providers_changed": {
      "$ref": "#/$defs/ProvidersChangedNotification"
    },
    "providers_usage_changed": {
      "$ref": "#/$defs/ProvidersUsageChangedNotification"
    },
    "request": {
      "$ref": "#/$defs/JsonRpcRequest"
    },
    "request_enrollment": {
      "$ref": "#/$defs/RequestEnrollmentParams"
    },
    "respond_approval": {
      "$ref": "#/$defs/RespondApprovalParams"
    },
    "response": {
      "$ref": "#/$defs/JsonRpcResponse"
    },
    "resume_session": {
      "$ref": "#/$defs/ResumeSessionParams"
    },
    "rotate_integration_key": {
      "$ref": "#/$defs/RotateIntegrationKeyParams"
    },
    "runtime_event": {
      "$ref": "#/$defs/RuntimeEventNotification"
    },
    "runtime_locator": {
      "$ref": "#/$defs/RuntimeLocatorRecord"
    },
    "runtime_terminal_id": {
      "$ref": "#/$defs/RuntimeTerminalId"
    },
    "runtime_terminal_view_id": {
      "$ref": "#/$defs/RuntimeTerminalViewId"
    },
    "session_descriptor": {
      "$ref": "#/$defs/SessionDescriptor"
    },
    "session_index_changed": {
      "$ref": "#/$defs/SessionIndexChangedNotification"
    },
    "session_index_ended": {
      "$ref": "#/$defs/SessionIndexEndedNotification"
    },
    "session_open_result": {
      "$ref": "#/$defs/SessionOpenResult"
    },
    "set_mode": {
      "$ref": "#/$defs/SetModeParams"
    },
    "set_model": {
      "$ref": "#/$defs/SetModelParams"
    },
    "start_session": {
      "$ref": "#/$defs/StartSessionParams"
    },
    "submit_blocks": {
      "$ref": "#/$defs/SubmitBlocksParams"
    },
    "submit_input": {
      "$ref": "#/$defs/SubmitInputParams"
    },
    "terminal_acquire_control": {
      "$ref": "#/$defs/TerminalAcquireControlParams"
    },
    "terminal_attach": {
      "$ref": "#/$defs/TerminalAttachParams"
    },
    "terminal_control": {
      "$ref": "#/$defs/TerminalControlParams"
    },
    "terminal_control_lease": {
      "$ref": "#/$defs/TerminalControlLease"
    },
    "terminal_descriptor": {
      "$ref": "#/$defs/TerminalDescriptor"
    },
    "terminal_detach": {
      "$ref": "#/$defs/TerminalDetachParams"
    },
    "terminal_exited": {
      "$ref": "#/$defs/TerminalExitedNotification"
    },
    "terminal_index": {
      "$ref": "#/$defs/TerminalIndexSnapshot"
    },
    "terminal_index_changed": {
      "$ref": "#/$defs/TerminalIndexChangedNotification"
    },
    "terminal_index_ended": {
      "$ref": "#/$defs/TerminalIndexEndedNotification"
    },
    "terminal_lagged": {
      "$ref": "#/$defs/TerminalLaggedNotification"
    },
    "terminal_open": {
      "$ref": "#/$defs/TerminalOpenParams"
    },
    "terminal_output": {
      "$ref": "#/$defs/TerminalOutputNotification"
    },
    "terminal_resize": {
      "$ref": "#/$defs/TerminalResizeParams"
    },
    "terminal_stop": {
      "$ref": "#/$defs/TerminalStopParams"
    },
    "terminal_view_opened": {
      "$ref": "#/$defs/TerminalViewOpened"
    },
    "terminal_write": {
      "$ref": "#/$defs/TerminalWriteParams"
    },
    "watch_enrollment": {
      "$ref": "#/$defs/WatchEnrollmentParams"
    },
    "watch_events": {
      "$ref": "#/$defs/WatchEventsParams"
    },
    "watch_events_result": {
      "$ref": "#/$defs/WatchEventsResult"
    },
    "watch_providers": {
      "$ref": "#/$defs/WatchProvidersParams"
    },
    "watch_providers_result": {
      "$ref": "#/$defs/WatchProvidersResult"
    },
    "watch_session_index": {
      "$ref": "#/$defs/WatchSessionIndexParams"
    },
    "watch_session_index_result": {
      "$ref": "#/$defs/WatchSessionIndexResult"
    },
    "watch_terminal_index": {
      "$ref": "#/$defs/WatchTerminalIndexParams"
    },
    "watch_terminal_index_result": {
      "$ref": "#/$defs/WatchTerminalIndexResult"
    }
  },
  "required": [
    "method",
    "request",
    "notification",
    "response",
    "initialize_params",
    "initialize_result",
    "challenge",
    "request_enrollment",
    "enrollment_receipt",
    "watch_enrollment",
    "enrollment_decision",
    "integration_grant",
    "rotate_integration_key",
    "runtime_locator",
    "provider_list",
    "provider_usage_list",
    "get_provider_capabilities",
    "provider_capabilities",
    "list_models",
    "model_catalogue",
    "list_native_sessions",
    "native_session_catalogue",
    "native_activity_params",
    "native_activity",
    "managed_session_list",
    "get_session",
    "session_descriptor",
    "start_session",
    "adopt_native_session",
    "resume_session",
    "session_open_result",
    "acquire_control",
    "control_lease",
    "control_lease_params",
    "cool_session",
    "forget_session",
    "delete_native_session",
    "archive_native_session",
    "submit_input",
    "submit_blocks",
    "set_model",
    "set_mode",
    "watch_events",
    "watch_events_result",
    "runtime_event",
    "lagged",
    "list_pending_approvals",
    "pending_approvals",
    "respond_approval",
    "watch_session_index",
    "watch_session_index_result",
    "session_index_changed",
    "session_index_ended",
    "watch_providers",
    "watch_providers_result",
    "providers_changed",
    "providers_usage_changed",
    "provider_watch_ended",
    "runtime_terminal_id",
    "runtime_terminal_view_id",
    "list_terminals",
    "terminal_index",
    "terminal_descriptor",
    "watch_terminal_index",
    "watch_terminal_index_result",
    "terminal_open",
    "terminal_attach",
    "terminal_view_opened",
    "terminal_control_lease",
    "terminal_acquire_control",
    "terminal_control",
    "terminal_write",
    "terminal_resize",
    "terminal_detach",
    "terminal_stop",
    "terminal_index_changed",
    "terminal_index_ended",
    "terminal_output",
    "terminal_lagged",
    "terminal_exited"
  ],
  "title": "PublicProtocolSchema",
  "type": "object",
  "x-runtrol-finalized-revisions": [
    "2026-08-27",
    "2026-08-13"
  ],
  "x-runtrol-limits": {
    "challengeLifetimeMs": 60000,
    "controlLeaseLifetimeMs": 30000,
    "enrollmentLifetimeMs": 600000,
    "idempotencyWindowMs": 86400000,
    "maxAttachmentBase64Bytes": 4194304,
    "maxFrameBytes": 16842752,
    "maxIdempotencyRecords": 2048,
    "maxInputBlocks": 16,
    "maxInputBytes": 1048576,
    "maxInputImages": 8,
    "maxModelSelectionBytes": 4096,
    "maxNativeAdoptionTokenBytes": 2048,
    "maxNativePublicCursorBytes": 8192,
    "maxPageItems": 100,
    "maxPendingEnrollments": 64,
    "maxReasoningSelectionBytes": 4096,
    "maxRevisionOffers": 16,
    "maxSubscriptions": 32,
    "maxTerminalColumns": 500,
    "maxTerminalIndexItems": 256,
    "maxTerminalOutputBytes": 4096,
    "maxTerminalRows": 200,
    "maxTerminalScreenBytes": 1048576,
    "maxTerminalViewQueueChunks": 128,
    "maxTerminalWriteBytes": 65536,
    "nativeCursorLifetimeMs": 300000
  }
}
